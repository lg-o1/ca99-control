# CA99 Control App — 统一 PWA

> 单一应用，所有玩法作为模块集中在这里，便于维护。
> 基于打分最高方案：PWA + Web MIDI（详见 `../docs/EVALUATION.md`）。

## 架构

```
app/
  index.html          ← 单页主界面（顶栏连接 + 侧栏模块切换）
  manifest.json       ← PWA manifest
  css/app.css         ← 样式
  js/
    midi-core.js      ← MIDI 连接层（双传输：Web MIDI 默认 / WebSocket 桥接走蓝牙）
    bridge-protocol.js ← 桥接协议（纯函数 + BridgeClient，midi-core 与 Python 桥共用）
    ca99.js           ← CA99 协议库（buildSoundSelect/buildSysEx/buildVT... 纯函数，可测试）
    ca99.test.mjs     ← 协议库单元测试（20 用例，node 运行）
    auto-rotate.js    ← 自动换音色引擎（纯逻辑，21 单元测试）
    vt-morph.js       ← VT 参数渐变引擎（纯逻辑，32 单元测试）
    velocity-switch.js← 力度感应换音色路由（纯逻辑，33 单元测试）
    vel-vt-link.js    ← 力度→VT 参数联动（纯逻辑，22 单元测试）
    pedal-control.js  ← 踏板控制扩展（纯逻辑，27 单元测试）
    preset-store.js   ← 演出预设存储（纯逻辑，40 单元测试，可注入存储后端）
    chord-detect.js   ← 和弦/音程识别（纯逻辑，46 单元测试）
    chord-trainer.js  ← 和弦练习挑战逻辑（纯逻辑，32 单元测试）
    metronome.js      ← 节拍器 + 演奏速度检测（纯逻辑，33 单元测试）
    metro-kit.js      ← 节拍器音色×拍号工具箱（6 拍号含复合分组重音 + 7 种 Web Audio 音效合成规格，纯逻辑，14 单元测试）
    recorder.js       ← 弹奏录制 + 标准 MIDI 文件编码（纯逻辑，38 单元测试）
    scale-trainer.js  ← 音阶练习引导（纯逻辑，41 单元测试）
    sight-reading.js  ← 视奏闪卡（五线谱→音名，纯逻辑，62 单元测试）
    ear-training.js   ← 音程听辨（听辨音程，纯逻辑，49 单元测试）
    dynamics-trainer.js ← 力度练习（弹出目标强弱，纯逻辑，52 单元测试）
    transposer.js     ← 移调器（一键升降调，纯逻辑，74 单元测试）
    practice-stats.js ← 练习成就仪表盘（聚合各模块成绩+成就，纯逻辑，51 单元测试）
    medals.js          ← 🏅 奖牌墙引擎（每首曲一枚 🥉🥈🥇💎，奖牌只升不降，配合收集面板，纯逻辑，13 单元测试）
heatmap.js         ← 🌡️ 练习热力图引擎（每个技能按「多久没练」着色 🟢🟡🔴⚪，按时间非正确率，纯逻辑，13 单元测试）
review-queue.js    ← 🔄 智能复习队列引擎（间隔重复：叠在 heatmap 的 per-skill 上次练习+次数上，用 Leitner/SM-2-lite 渐长间隔算「今天该复习哪几项」，练得越多间隔越长；从未练的归「待探索」邀请不施压，纯逻辑，14 单元测试）
theme.js           ← 🌈 全局界面主题引擎（5 套深色主题，切 <html data-theme> 一次换全 app 配色；只覆盖外观/品牌色 token，语义反馈色红=错/绿=对/黄=提示跨主题不变；持久化到 localStorage ca99_theme，纯逻辑，16 单元测试）
microstars.js      ← 🌟 成长之星引擎（每个技能拆 8 颗小星，按累计练对数点亮，只升不降，纯逻辑，12 单元测试）
    streak-calendar.js ← 🔥 打卡火焰日历引擎（把连胜引擎可视化为火焰日历网格，按当日练习次数定火焰强度，含豁免券补救，纯逻辑，14 单元测试）
    mystery-box.js     ← 🎁 惊喜盲盒引擎（每次练习按概率开盒解锁好玩音色，攒「音色图鉴」，可注入 storage+rng，纯逻辑，11 单元测试）
    daily-goal.js      ← 🎯 每日自选微目标引擎（按日期确定性挑 2-3 个推荐玩法让孩子自选，练 1 次即达成，SDT 自主感，可注入 storage，纯逻辑，12 单元测试）
    share-card.js      ← 🎬 演奏分享卡引擎（从录制事件还原音符/汇总/⭐ 鼓励向评星/挑鼓励语/迷你卷帘布局，给 canvas 画分享卡，纯逻辑，20 单元测试）
    parent-weekly.js   ← 📊 家长周报引擎（聚合 practice-stats 每日练习/各模块/连胜/正确率 + heatmap 覆盖面，成「本周亮点」家长卡：splitWeeks 切上周/本周 + trendOf 成长向趋势 + topModules + coverageOf + highlightLines + parentPraise，纯逻辑，17 单元测试）
    staff-wars.js      ← 🚀 看谱击落引擎（音符入侵者飞向炮台、读谱弹键击落、丢命/升级/提速，按音名匹配[可选精确]，`revive()` 满血复活保留得分，可注入 rng/dt，纯逻辑，18 单元测试）
    drops.js           ← 🫧 接音水滴引擎（音符水滴下落、弹对接住、连击/最佳连击、漏到地面只断连击不结束，按音名匹配[可选精确]，可注入 rng/dt，纯逻辑，12 单元测试）
    cof-puzzle.js      ← 🧩 五度圈拼图引擎（C→G→D 顺时针闯关、弹对整条大调音阶解锁该格、进度持久化、按音高类匹配[可选精确]，复用 circle-of-fifths.scaleMidi，纯逻辑，12 单元测试）
    rhythm-trainer.js ← 节奏跟拍训练（按拍点敲击判时间误差，纯逻辑，119 单元测试）
    melody-dictation.js ← 旋律听写（听调内短旋律后复奏，逐音校验，纯逻辑，486 单元测试）
    chord-progression.js ← 和弦进行练习（著名进行展开成具体和弦按序弹，复用和弦识别，纯逻辑，120 单元测试）
    beat-stability.js ← 节拍稳定度分析（采集击键间隔算稳定度/估BPM/赶拍拖拍，纯逻辑，58 单元测试）
    hands-sync.js     ← 双手协调练习（按音高分左右手，测每拍双手落键时间差算协调度，纯逻辑，58 单元测试）
    arpeggio-runs.js  ← 琶音跑动速度测试（根音+性质+八度+方向生成目标琶音，按序弹测速度/均匀度评分，纯逻辑，90 单元测试）
    articulation.js   ← 连奏/断奏控制（用按住时长÷起音间隔的触键比判连奏/断奏并逐音打分，纯逻辑，60 单元测试）
    pedal-timing.js   ← 踏板配合时机（切分踏板法，测新音到重新踩下的间隔判干净/脏/干，纯逻辑，70 单元测试）
    trill.js          ← 颤音速度训练（两音快速交替，测颤音速度Hz+均匀度+交替正确性，纯逻辑，77 单元测试）
    ornament.js       ← 装饰音训练（倚音/波音/回音，按目标音序列逐音匹配+测干脆度/均匀度，纯逻辑，88 单元测试）
    leap.js           ← 音程大跳准确度（生成大跳序列，逐音跳准，测一次弹准率，纯逻辑，70 单元测试）
    voicing.js        ← 旋律声部突出（和弦里目标声部力度是否高于内声部，纯逻辑，63 单元测试）
    crescendo.js      ← 力度渐变曲线（一串音的力度走向是否平滑渐强/渐弱，测方向/平滑度/跨度，纯逻辑，70 单元测试）
    tempo-ramp.js     ← 速度渐变（一串击键间隔是否平滑渐快/渐慢，测方向/平滑度/变速幅度，纯逻辑，72 单元测试）
    polyrhythm.js     ← 复节奏（两声部 a:b 平分周期，按音高/按钮分左右手分别判命中，纯逻辑，66 单元测试）
    evenness.js       ← 颗粒性（一串跑动音的力度/时值是否均匀，用变异系数 CV 打分，纯逻辑，57 单元测试）
    finger-independence.js ← 手指独立性（按住几个键不放、动其他手指，测按住音是否滑脱+音型对错，纯逻辑，44 单元测试）
    scale-span.js     ← 音阶八度跨度（音阶连跑 2~3 个八度，测音符正确+速度均匀+穿指衔接，纯逻辑，62 单元测试）
    rhythm-dictation.js ← 节奏听写（听一段节奏后凭记忆敲回来，与速度无关只比长短比例，纯逻辑，60 单元测试）
    sight-transpose.js ← 移调视奏（看 C 调小旋律移到目标调弹出来，测移调音级正确率+旋律形状，纯逻辑，55 单元测试）
    chord-inversion.js ← 和弦转位听辨（听一个三和弦，判断原位/第一/第二转位，纯逻辑，47 单元测试）
    key-signature.js  ← 调号识别（看调号几个升/降号，判断大调/小调，五度圈，纯逻辑，402 单元测试）
    scale-fingering.js ← 音阶指法提示（显示标准音阶指法、跟弹高亮、穿指点，纯逻辑，113 单元测试）
    interval-build.js  ← 音程构建（给根音+音程名，弹出目标音，纯逻辑，48 单元测试）
    mode-id.js         ← 调式识别（听教会调式音阶多选辨认，纯逻辑，146 单元测试）
    solfege.js         ← 唱名/音级听辨（建立调性后辨认音级 Do-Re-Mi，纯逻辑，196 单元测试）
    chord-quality.js   ← 和弦性质听辨（听和弦辨大/小/增/减/七和弦类型，纯逻辑，185 单元测试）
    progression-ear.js ← 和声进行听辨（听大调进行辨每个和弦的罗马数字级数，纯逻辑，337 单元测试）
    cadence.js         ← 终止式辨认（听两个和弦的终止式辨正格/变格/半终止/阻碍，纯逻辑，287 单元测试）
    note-id.js         ← 键盘音名认知（音名↔88键位双向认键：看音名找键/看键认音名，纯逻辑，1182 单元测试）
    staff-read.js      ← 五线谱识谱卡（看谱认音名：看谱选音名/看谱点键、带线间口诀、高音/低音/大谱表，纯逻辑，2270 单元测试）
    sight-phrase.js    ← 乐句视奏（读一句标准记谱短旋律：带节奏+调号，照谱逐音弹奏判分，8 调/3 档节奏，纯逻辑，21 单元测试）
    chord-sight.js     ← 和弦视奏（读五线谱上的叠置和弦：三/七和弦、可转位、带调号，整组同时按下判分并报和弦名，纯逻辑，21 单元测试）
    rhythm-sight.js    ← 节奏视奏（读单行节奏谱：随机生成各拍时值+休止符，节拍器预备拍后照谱击打任意键/TAP，判分完美/良好/漏击/多击+抢拍拖拍倾向+落点时间对比图，4/4·3/4·3 档难度，纯逻辑，20 单元测试）
    accompaniment.js   ← 伴奏音型（把所选调上的和弦进行用柱式/阿尔贝蒂低音/华尔兹/分解琶音/行进低音五种伴奏型展开成"该弹的音"步骤，照高亮按顺序弹出即推进，步进式判分+连击+评星，复用 chord-progression 调与进行库，纯逻辑，21 单元测试）
    chord-color.js     ← 和弦色彩板（弹下任意和弦→识别和弦名+和声功能，映射成颜色光：大调暖/小调冷/属七减和弦闪烁"想回家/紧张"，设调后给出罗马级数 I/ii/V7 与功能名+"解决回主"提示，复用 chord-detect，纯逻辑，24 单元测试）
    melody-echo.js     ← 旋律回声（Simon 记忆游戏：亮键+发声播放短旋律→照弹回来→对了旋律自动加长一个音，越记越长；五音/五声/八度/半音阶四关，纯逻辑序列生成/比对/计分/生长机制，20 单元测试）
    call-response.js   ← 即兴问答（Call &amp; Response：钢琴弹开放问句→你即兴答句回应，按"留在音阶+落回主音+有起伏"音乐性评分，不要求照抄；五声/大调/小调四关，纯逻辑问句生成/答句评分，24 单元测试）
rhythm-echo.js     ← 节奏回声（节奏版 Simon：亮灯+打点播放一段节奏型→你在琴键/空格/敲击区拍回来→对了节奏自动加长一个音，越拍越长；判定与速度无关，只看长短比例；长短音/含八分/混合/含附点四关，纯逻辑节奏生成/拍点归一化比对/状态机，21 单元测试）
pitch-direction.js ← 高低音方向感（零基础音高启蒙：弹一个参考音+方向⬆️/⬇️→你在琴上随便弹一个更高/更低的音，方向对且差得够明显就算对，不要求弹准某个音；大跳/五度/三度/一步之遥四关，连击越高门槛越小，纯逻辑出题/方向判定/状态机，18 单元测试）
    piano-keyboard.js  ← 通用全幅 88 键虚拟钢琴组件（可点击发声/高亮答案/演示点亮，纯布局逻辑 46 单元测试）
    score-follow.js    ← 曲谱跟弹（Synthesia 式：内置 7 首乐曲/上传 MIDI 落音符到琴键、3 档训练、音名标签/节拍器/渐进提速/提示/区间循环/落点时间对比图、左右手分色/和弦/变速、力度可视化/真琴发声/每曲历史最高分/三星彩屑庆祝/随机选曲/练习足迹统计，等待模式支持「🌟 容差通过」近音放行，纯逻辑，44 单元测试）
    midi-file.js       ← 标准 MIDI 文件(SMF)解析器（解析多轨/VLQ/note on-off/running status/tempo map，tick→ms，自动按音轨或音高分配左右手，并捕获各通道/音轨的 program-change 音色号供乐队伴奏分配乐器，纯逻辑，13 单元测试）
    backing-band.js    ← 🎸 乐队伴奏引擎（Tomplay 式：把多轨 MIDI 按通道/音轨拆成声部、用 GM program 号映射乐器音色家族、推荐旋律声部为"我来弹"、生成"我的部分静音 + 其余声部当乐队"的播放计划，纯逻辑，11 单元测试）
    userlib.js         ← 我的曲库（自定义曲库：解析目录自动索引页或 userlib.json 清单→构建分类曲目目录，子目录即分类，纯逻辑，11 单元测试）
    midi-player.js     ← 钢琴卷帘播放器引擎（DAW 式音块布局/音高范围八度对齐/播放头/某时刻触发音与发声音/统计，复用 midi-file 解析的 MIDI，纯逻辑，11 单元测试）
    staff-view.js      ← 五线谱播放器引擎（MIDI→大谱表布局：音名→谱级/升号/谱号判定/加线计算/小节线/播放光标，纯逻辑，14 单元测试）
    circle-of-fifths.js ← 五度圈交互工具（点调看调号/关系小调/正确拼写音阶/顺阶和弦 I–vii°，可试听音阶+和弦+终止式，纯逻辑，21 单元测试）
    note-color.js      ← 彩色音符脚手架（启蒙识谱按音名上色 C 红…B 紫/掌握度淡出 scaffoldStrength，视奏闪卡+识谱卡共用，纯逻辑，7 单元测试）
    boss-battle.js     ← 🐉 Boss 战引擎（把难乐句做成打怪：按顺序弹对削血/弹错掉心/连续完美评星/忽略八度，`revive()` 满血复活保留削血进度，纯逻辑，11 单元测试）
    revive.js          ← 💛 复活币引擎（每日温和补充的「二次机会」安全网，降挫败：DAILY_REVIVES=5/dayKey 本地日期/ReviveBank coinsLeft·canRevive·useRevive·reset，跨天自动补满、可注入 store 持久化，纯逻辑，12 单元测试）
    speed-run.js       ← 🚀 极速挑战引擎（整段乐句计时换算有效 BPM/达标升档+5/刷个人纪录/弹错从头/不跟节拍器，纯逻辑，8 单元测试）
    dice-warmup.js     ← 🎲 骰子热身引擎（掷骰随机生成 调性×音型×手别×风味 任务卡/生成音序/逐音校验/忽略八度，纯逻辑，12 单元测试）
    bingo-card.js      ← 🎯 练习宾果引擎（5×5 多样任务格/Fisher–Yates 洗牌/行列斜线连线检测/免费中心格/填满判定，纯逻辑，11 单元测试）
    guess-song.js      ← 🕵️ 猜歌视奏引擎（公有领域童谣旋律/藏曲名照谱弹/回放/4 选项猜歌/逐音校验/忽略八度，纯逻辑，12 单元测试）
    timbre-guess.js    ← 🎨 音色猜猜乐引擎（CA99 硬件独家：23 种可辨识乐器图鉴按家族分组/resolveInstruments 映射真实音色 id/makeRound 优先同家族干扰/连对加成/答错不扣分，纯逻辑，16 单元测试）
    velocity-coaster.js ← 🎢 力度过山车引擎（CA99 硬件独家：6 种起伏轨道形状[小山坡/山谷/楼梯/波浪/锯齿]/heightToVelocity 高度↔力度互换/judgeHit 按 velocity 差评级 perfect/good/ok/miss/VelocityCoaster 逐站行进计分连击星级，复用 dynamics-trainer 力度档，纯逻辑，19 单元测试）
    tone-trees.js      ← 🌲 和弦寻宝引擎（给和弦名找全组成音点亮树：chordPitchClasses 由 chord-detect 模板算组成音级/TREE_CHORDS 19 题按 1-3 级[白键三和弦/含黑键 sus/七和弦]/pickChord 避免连出同张/ToneTreeGame 八度无关逐音点亮·非和弦音不扣分，纯逻辑，14 单元测试）
    paddle-tones.js    ← 🏓 弹球接音引擎（Theta 最上瘾玩法搬到 88 键：POOLS 3 级音池[五声/C大调/半音]/buildBalls 等距出生避免连续重复/PaddleTones 音球下落 yOf·play 接住可接区内[catchTop0.45~catchBottom1.12]音级匹配最靠近地面的球·perfect≥0.78·弹错音 stray 断连击不扣分·expire 漏接断连击无 game-over·octave-agnostic·catchRate/stars，纯逻辑，14 单元测试）
    rhythm-puzzles.js  ← 🧩 节奏拼图引擎（柯达伊节奏卡 ta/ti-ti/ta-a/休止/tika-tika/ti-tika 含 beats/level/cells；cardsUpToLevel 按难度解锁/flattenCards/signature 结构指纹/beatsOf/onsetCells 起音格/generatePuzzle 随机出题/RhythmPuzzle place·removeAt·pop·clear·isFull·isCorrect[按结构指纹比对故等价摆法也算对]·check·progress，纯逻辑，13 单元测试）
    weekly-quest.js    ← 🗓️ 每周挑战赛季引擎（6 主题轮换/周一归位/周键/主题确定性轮换/周倒计时/任务自动计数封顶，纯逻辑，14 单元测试）
    ghost-race.js      ← 👻 幽灵竞速引擎（4 段乐句赛道/对过去最佳计时/幽灵进度插值/projectedMs 预测/领先判定/奖牌分级，纯逻辑，16 单元测试）
    rhythm-jump.js     ← 🥁 节奏跳跳引擎（太鼓达人式：5 节奏型/拍位时刻表/任意键拍击/完美·不错·漏拍判定窗口/空挥不扣连击/命中率评星，纯逻辑，16 单元测试）
    family-duel.js     ← 👯 双人对战引擎（家庭轮流上场比总分：turnOrder 交替/startTurn/press 弹对+1·弹错不扣分/endTurn/result 胜负或平局/teamTotal 合作总分，八度无关，纯逻辑，15 单元测试）
    sound-paint.js     ← 🎨 音画涂鸦引擎（每弹一音映射成一笔光斑：音高→色相+横位/时间→纵位/力度→大小·透明度，SoundPainting 收集 strokes/palette/summary，零失败无评分，纯逻辑，13 单元测试）
    pet-grow.js        ← 🐣 养成小伙伴引擎（随累计练习成长/进化的音乐宠物：8 进化阶段/petXp 复用经验系统/stageFor/progressToNext/moodFor 温和不惩罚/petSummary 含进化检测，纯逻辑，14 单元测试）
    loop-composer.js   ← 🎼 循环作曲台引擎（多层叠录把即兴攒成一首歌：bars/beatsPerBar/bpm/quantizeDiv 网格量化、addLayer/toggleMute 多层、addNote 量化吸附起音、events 合并未静音层为有序 on/off、toMidiFile 合并 Type-0 SMF 带重复，复用 recorder.js 的 SMF 原语，纯逻辑，14 单元测试）
    warmup-routine.js  ← 🌅 每日热身例程引擎（一键今日 5 分钟例程：pickRoutine 按日期确定性组出「音阶+节奏+小曲」三步、复用 dice-warmup 音阶卡/metro-kit 拍号/daily-goal hashStr/现成歌曲目录、WarmupRoutine 跟踪三步进度+音阶逐音校验+按日打卡累计 totalDone，纯逻辑，16 单元测试）
    play-mood.js       ← 🎭 情绪演奏引擎（给一张情绪卡[开心☀️/神秘🌙/暴风雨⛈️/平静🌊/悲伤💧/魔法✨]，孩子用力度+速度+音色自由表达，零对错——PlayMood 收集自由演奏的力度/时长/音域、reflect() 永远鼓励向地点评「贴合情绪」、复用 dynamics-trainer 力度档 + chord-color 情绪色，纯逻辑，15 单元测试）
    loop-trainer.js    ← 🔁 AB 循环慢练器引擎（分段精练：框选 A→B 小节只循环这段 + 从慢速起步每弹干净一遍自动 +10% 爬到 100% 原速 + 左右手分层——SpeedLadder 速度阶梯/measureWindow 小节→拍窗/LoopSession 逐组等待推进与干净遍计数，复用 score-follow 的 waitMatch 容差匹配/groups/handFilter，纯逻辑，20 单元测试）
    melody-palace.js   ← 🧠 旋律记忆宫殿引擎（渐进 Simon：用一首真实曲子的旋律，每轮只揭示前 N 个音→照弹回来→对了再多放一个音，逐音累积凭耳朵学会整首曲子——pitchesFromSeq 音高提取/noteMatch 忽略八度+容差比对/MelodyPalace 渐进状态机 showing→input→win/mastered，纯逻辑，24 单元测试）
    daily-song.js      ← 🎲 今日推荐曲引擎（从内置 1500+ 曲库按日期确定性推一首：dayKey 本地日期键/hashStr FNV-1a 哈希/pickDailyIndex 同日同首+换一首 salt 跳号/prettyName 文件名兜底清洗/catEmoji 分类图标，纯逻辑，11 单元测试）
    song-tree.js       ← 🌳 曲库层级树引擎（把扁平 catalog 整理成「顶层分类 fn → 子分类 cat → 曲目」两级：buildSongTree 分组+保序+单子类标记、findNode/findSub 定位、searchSongs/countMatches 跨库扁平搜索。供「分类 › 子分类 › 选曲」层级弹窗复用，纯逻辑，11 单元测试）
    sheet-index.js     ← 📖 课本谱面叠加引擎（当一首 .mid 旁有同名文件夹含 index.json + 每小节照片切片 m001.png…，把真实课本谱面拼成横向卷帘 + 跟随播放高亮当前小节：parseSheetIndex 规范化小节并累加 x0/x1 像素偏移、measureAtBeatRange 按每小节真实 MIDI 拍区间 [beat_start,beat_end) 二分查找定位光标[弱起/变拍/反复 D.S. 均稳，反复段由 OMR 展开成重复条目→卷帘顺演奏序平铺，光标只向前不回跳]、measureAtBeat 整曲均匀映射[当 index.json 缺 beat_start/beat_end 时的回退]、cursorX 算光标源像素位、sheetPaths/pngUrl 路径派生；显示高度与切图源高度解耦——defaultSheetHeight 按视口高×比例自适应[横屏收窄/竖屏放大]、clampSheetHeight 夹到 [72,360] 供手动滑杆调节，纯逻辑，23 单元测试）
    app.js            ← 主入口 + 各玩法模块
  data/               ← 从逆向数据生成的 CA99 专属精简表
    sounds.json       ← 346 CA99 音色（name/category/pc/msb/lsb）
    sysex.json        ← 526 CA99 SysEx 参数定义
    vt.json           ← VT 参数
    rhythm.json       ← 100 节奏
```

## 已实现模块（玩法）

> 侧边栏按「演奏控制 / 练习训练 / 工具」三组分类，每组标题带模块数量徽章，<b>顶部带难度过滤条</b>（🌱 初级 / 🌿 进阶 / 🔥 全部，默认<b>初级</b>，记忆到 localStorage）——模块多达 82 个，默认只列出 35 个适合零基础的「初级」玩法，避免一上来就被复杂功能淹没；切「进阶」加显中级（共 61 个）、「全部」显示所有。每个 `.nav-btn` 用 `data-level="1|2|3"` 标级，`setupNavSearch()` 里 `levelOk()` 按「≤ 当前级别」过滤（初级=1、进阶=1+2、全部=不限）。其下是 🔍 实时搜索框（输入即过滤模块，Esc 清空）——<b>搜索时跨全部级别</b>（显式搜索意图优先，即便处于初级也能搜到高级模块），清空后恢复按级别过滤。可滚动；切换模块时顶栏面包屑显示当前模块名、内容区自动回到顶部；窄屏（≤1024px 收窄、≤720px 折叠为图标条）自适应。各模块标题带渐变色条、统计数字用渐变描字，整体配色/动画统一。

| 模块 | 状态 | 说明 |
|------|------|------|
| 🎵 音色浏览器 | ✅ | 346 音色，按分类筛选/搜索，点击切换（标准 Bank Select + PC）。下方配<b>全幅 88 键虚拟钢琴</b>，切音色后直接点键即发真实 MIDI 试听，不用去琴上弹就能判断音色对不对 |
| 🔧 VT 调音台 | ✅ | 42 个 Virtual Technician 参数，滑块/下拉实时调 |
| 🎛️ 系统/混响 | ✅ | 音量/混响类型/键盘模式 |
| 🥁 节奏 | ✅ | 100 鼓点节奏选择 |
| 🔄 自动换音色 | ✅ | 定时(每N秒)或按节拍(弹N个音符)循环切换音色，可选音色池+顺序/随机 |
| 🌗 VT 渐变器 | ✅ | CA99 独有：边弹边把共鸣/击弦等 VT 参数从起点平滑插值到终点（缓动/往返循环） |
| 🎚️ 力度换音色 | ✅ | 按弹奏力度自动切换音色（2-4 层），轻弹/重弹不同音色，演奏更有层次 |
| 💫 力度→VT联动 | ✅ | CA99 独有：弹奏力度实时驱动 VT 参数（越重击弦共鸣越强），带平滑防抖 |
| 🦶 踏板控制 | ✅ | 实时显示三踏板（延音/保持/弱音）深度，可把踏板深度映射到 VT 参数 |
| ⭐ 演出预设 | ✅ | 把音色+VT 调音组合命名保存，一键调用；localStorage 持久化 + JSON 导入导出 |
| 🎓 和弦练习 | ✅ | 实时识别弹奏的和弦/音程（含转位），挑战模式按提示弹和弦闯关计分 |
| 🎵 节拍器 | ✅ | 可视+可听节拍器：<b>6 种常见拍号</b>（2/4 进行曲 / 3/4 华尔兹 / 4/4 / 5/4 / 6/8 摇篮曲 / 7/8，复合拍号<b>正确分组</b>——强拍 accent / 次强 mid / 弱拍三级重音，6/8 重音落在第 1、4 拍）+ <b>7 种可选音效</b>（🔔经典电子 / 🪵木鱼 / ⏱️清脆嘀嗒 / 🥁鼓 / 🛎️铃铛 / 👏拍手 / 🐤啾啾鸟，可「试听」整小节）；强/次强/弱拍用不同颜色脉冲点显示。弹奏时实测你的实际速度 BPM。音效偏好<b>三处共享</b>（独立节拍器 + 两个 Synthesia tab）并 localStorage 持久化。<b>纯浏览器 Web Audio 合成，绝不发往 CA99</b>。引擎 `metro-kit.js`（拍号重音模式/音效合成规格，14 单元测试） |
| 🎡 五度圈 | ✅ | 交互式<b>五度圈</b>乐理中枢：SVG 圆盘外环 12 大调 / 内环关系小调，顺时针每格升五度（多 1♯）、逆时针降五度（多 1♭）。点任意调即显<b>调号</b>、<b>关系小调</b>、正确拼写的<b>音阶</b>（如 G→F#、Db→全降）与<b>顺阶三和弦 I ii iii IV V vi vii°</b>（罗马数字按大/小/减三色标）。可🔊播放音阶、点和弦试听、听 <b>I–IV–V–I 终止式</b>，钢琴上同步高亮（主音金色）。相邻调只差一个音——转调/扒谱/即兴配和声的核心地图，接 CA99 可直接弹真琴 |
| ⏺ 录制回放 | ✅ | 录下弹奏，回放欣赏，或导出标准 MIDI 文件（.mid）保存/分享 |
| 🎼 音阶练习 | ✅ | 选调+音阶类型，按高亮提示依次弹奏，实时检查对错+进度（8 种音阶/上下行/忽略八度） |
| 👀 视奏闪卡 | ✅ | SVG 五线谱出题，看谱在琴键弹出对应音，弹对自动出下一题（高/低音谱号/连击/正确率/忽略八度） |
| 🎹 曲谱跟弹 | ✅ | <b>Synthesia 式</b>跟弹，<b>支持上传任意 .mid/.midi 文件</b>作为曲谱（自动识别多轨/和弦/左右手/变速），更内置 <b>📚 CA99 自带曲库（1543 首）</b>——把钢琴出厂自带的车尔尼/拜厄/布格缪勒/哈农/巴赫创意曲/小奏鸣曲/肖邦/成人课程/Alfred 教程/赞美诗等用<b>真实曲名+作曲家</b>（取自 APK 的 <code>music.json</code>）按<b>七大类（教程练习/钢琴名曲/Concert Magic/赞美诗/放松/音色示范/其他）</b>组织，每个大类下再细分<b>子分类</b>（如教程练习含 拜厄106/车尔尼100/哈农/巴赫创意曲/肖邦圆舞曲… 24 个子类）。选曲改为<b>统一弹窗</b>（和「🎬 演奏台」「🎸 乐队伴奏」同款 popup，点「📚 打开曲库」展开），用<b>层级面包屑</b>逐级深入：<b>分类 › 子分类 › 选曲</b>（单子类的分类一步直达曲目），任意层都能输关键词<b>跨全库搜曲名/作曲家/分类</b>，点一下即 fetch 对应 .mid 载入跟弹并自动关窗（解析失败的那首会友好提示换一首）；同一弹窗内切到 <b>🎵 我的 MIDI（自定义）</b>——把你自己整理的 .mid 按「<code>app/data/&lt;分类&gt;/歌曲.mid</code>」（子目录即分类）放好，从 app/ 目录起服务后点「🔄 重新扫描」即同样层级浏览/搜索/点击载入（静态主机可用 <code>scripts/build_user_catalog.py</code> 先生成 <code>userlib.json</code> 清单）；另外<b>任何模式下弹真琴，屏幕 88 键都实时镜像点亮</b>（接 CA99 即"屏幕镜像真琴"）。也可选内置曲目：<b>🐣 4 个零基础启蒙关卡</b>（找中央C/低·中·高C 音区认知/高低音方向感/五指阶梯 C-D-E-F-G——选中即自动开音名标签并提示用 🐢 等待练习，把"找中央C/音区/方向感"这些最缺的零基础启蒙环节直接融进有趣的 Synthesia 玩法，而非另开模块）+ <b>7 首乐曲</b>（小星星/欢乐颂/玛丽的小羊/铃儿响叮当/🎼巴赫小步舞曲G/🎼致爱丽丝/🎼肖邦夜曲Op.9No.2）。音符像 Synthesia 从上往下<b>落到对应琴键</b>，块上直接标<b>音名</b>（C4/E4…可关），上方<b>五线谱</b>同步走光标（右手蓝/左手紫，带图例）。<b>三档由易到难的训练</b>：🔊 <b>听示范</b>（看+听不判分）→ 🐢 <b>等待练习</b>（播放头停在当前音符组、弹齐才前进，最适合入门，卡住按 💡 <b>提示</b>让该弹的键闪 3 秒并发声；默认开 <b>🌟 容差通过</b>——MIDI 偶有抖动或弹偏 1–2 个半音时，只要节奏对就<b>温和放行并提示"差一点点～已经帮你过啦"</b>而非彻底卡住，避免完美音才能过让孩子受挫，关掉即恢复严格精确）→ 🎯 <b>跟弹判分</b>（音符落到判定线那刻弹对应键，按<b>音高+时机</b>双重判分：正中=PERFECT、稍偏=GOOD、漏弹=MISS，连对累计 Combo，结束按正确率给 ★☆ 评星）。可开 🥁 <b>节拍器</b>（跟随播放头按拍点击、重拍加重音、显示实时 BPM 徽章；<b>等待练习</b>时改用<b>自由走拍的浏览器节拍器</b>——播放头冻结等你弹对时节拍照样响，孩子卡住也能跟着拍子，不经 CA99 只在浏览器响）、🐇 <b>渐进提速</b>（每遍正确率 ≥80% 自动 +0.05× 直到 1.5×）、🔁 <b>区间循环</b>（选「第 X–Y 小节」反复练某个难点段落，五线谱上高亮循环区，跳回段首不自动结束、停止时记成绩）。完成后画 ⏱️ <b>落点时间对比图</b>（每个音点在「准点线」上下：<b>抢拍</b>在上、<b>拖拍</b>在下，颜色按 PERFECT/GOOD/MISS 区分，附平均误差、最大误差与「偏抢/偏拖/均衡」总评，知道自己习惯抢拍还是拖拍）。MIDI 含左右手时可选<b>练右手/左手/双手</b>（其余手音符淡显）。可选速度（0.5/0.75/1×）、难度（简单忽略八度/标准要弹准八度），没接 MIDI 也能点屏幕琴键作答，成绩入仪表盘。还有更多趣味开关：💪 <b>力度可视化</b>（上传 MIDI 自带 velocity 时强音更亮、弱音更暗，一眼看出原曲强弱起伏）、🎹 <b>真琴发声</b>（接 CA99 时示范/提示直接用钢琴当前音色在真琴上发声，更沉浸）、<b>每曲历史最高分</b>（用 localStorage 存每首的最佳「★★★ 92% 练过 N 遍」，切歌即显，刷新纪录有 🏅 提示）、🎲 <b>随机一首</b>（换口味时帮你随机挑一首没在练的）、三星满分时撒一阵 🎉 <b>彩屑庆祝</b>，<b>每弹对一个音</b>还会在对应琴键位置迸发一束 ✨ <b>彩色火花</b>、判定线随命中<b>发光脉冲</b>——而且火花<b>跟着你的 MIDI 力度走</b>：轻弹少量冷蓝小火花、重弹一大片暖红大火花，判定线发光也随力度增强（接 CA99 时直接把触键力度画成声光），连对到 5/10/15… 时弹出 🔥 <b>连击里程碑</b>飘字（可用 ✨ 击中特效开关一键关掉）。<b>🐣 启蒙关卡</b>还会在下落块和琴键上标出<b>手指号</b>（1=拇指…5=小指，如五指阶梯 C-D-E-F-G 标 1·2·3·4·5），零基础照着按。完成后还会推荐去 🎼 <b>乐句视奏</b>脱离下落提示纯读谱练同一句，形成识谱闭环。底部 🎼 <b>练习足迹</b>面板用 localStorage 记录每遍计分演奏：<b>每首练了几次</b>（条形排行 + 最佳星级/正确率 + 最近练习时间）、<b>近 7 天每日练习量</b>柱状图、<b>本周练最多</b>是哪首，一眼看清自己的练习分布。另有 📷 <b>拍谱识别（实验）</b>入口：填入本机 OMR 服务地址（如 homr，接收 <code>image</code> 表单字段、返回标准 MIDI），上传/拍一张乐谱图片即自动识别成可跟弹的曲目；无服务时友好提示改用「上传 MIDI」。还有 📖 <b>课本谱面叠加</b>：当所选曲目旁存在与 .mid <b>同名的文件夹</b>（内含 <code>index.json</code> + 每小节照片切片 m001.png…，由你的 OMR 切图流程生成），选曲器该行会显示 <b>📖 谱</b> 角标，载入后在五线谱上方多出一条<b>真实课本谱面卷帘</b>——把原书照片按小节拼接，<b>播放/跟弹时光标跟随高亮当前小节</b>并自动横向滚动居中，识别可能不准的小节淡黄标注，可 ◀▶ 手动翻看。这样孩子<b>看 app 一块屏</b>就能同时看到落音光柱 + 88 键 + 合成五线谱 + <b>真正的课本五线谱</b>，不必再边弹边翻纸质书 |
| 👂 音程听辨 | ✅ | 电脑发声播放两个音，辨认它们的音程并点按钮作答（上行/下行/和声/混合，可选音程范围，连击/正确率） |
| 💪 力度练习 | ✅ | 屏幕给目标力度（pp~ff），用触键强弱命中它，力度刻度条+指针实时显示你的 velocity（6 档/容差可调/连击/正确率） |
| 🎹 移调器 | ✅ | 一键把整个键盘升/降调（-12~+12 半音），用熟悉指法弹任意调，发 CA99 移调 SysEx 让钢琴自身也移调（滑块/±按钮/预设/听感调显示） |
| 🥁 节奏跟拍 | ✅ | 屏幕给节奏型（四分/八分/切分/附点），先一小节预备拍（节拍器引导），跟着拍点在琴键敲击，按时间误差判完美/良好/漏拍/多敲，播放头+落点高亮，平均误差统计（无琴可空格键敲） |
| 🎼 旋律听写 | ✅ | 听一段调内短旋律（首音为主音锚点，Web Audio 三角波播放），在琴键上逐音复奏，对的灯变绿、错的不前进，整条全对自动出下一条；可选调/长度/忽略八度/速度，带屏幕琴键，可再听/放弃看答案，成绩入仪表盘 |
| 🎹 和弦进行 | ✅ | 把"万能流行 I–V–vi–IV""ii–V–I""卡农""12 小节布鲁斯"等 7 条著名进行在所选调（含大小调）上展开成具体和弦（C–G–Am–F），按序弹出每个和弦即推进（忽略转位，复用和弦识别），罗马数字+和弦名高亮，可试听整条/替我弹演示，循环计圈，成绩入仪表盘 |
| 📈 节拍稳定度 | ✅ | 持续均匀弹奏（或空格键敲击），采集击键间隔算出稳定度评分（基于变异系数 CV）、估算 BPM、判断赶拍/拖拍/稳定；可开跟拍模式对照固定 BPM 测准度（带节拍器试听），实时间隔条形图（绿/黄/红显示偏差），成绩入仪表盘 |
| 🙌 双手协调 | ✅ | 每拍同时弹一个低音区（左手）和一个高音区（右手）音，按音高分手，测两手落键时间差（onset spread）算协调度评分，圆形仪表+逐拍色点显示，统计平均协调/双手到齐率/连击/最佳，成绩入仪表盘 |
| 🎶 琶音跑动 | ✅ | 选根音+和弦性质（大/小/属七/大七…）+八度+方向（上/下/上下行）生成目标琶音音序，按序弹出，引擎测速度（音/秒）与均匀度（IOI 变异系数）综合评分，目标音序高亮推进，**88 键键盘高亮目标音序（紫色目标+黄 ▶ 当前音，可点键试弹）**，成绩入仪表盘 |
| 🎻 连奏断奏 | ✅ | 选目标演奏法（连奏 legato / 断奏 staccato），连续弹音，引擎用每个音的按住时长÷到下一音的间隔得"触键比"判连贯/短促并逐音打分，色块分数墙显示，**88 键键盘实时显示按住的音（连奏重叠点亮、断奏短促闪过，绿/红=达标与否）**，成绩入仪表盘 |
| 🦶 踏板时机 | ✅ | 练切分踏板法（连奏踏板）：弹新音后抬延音踏板再重新踩下，引擎测"新音→重新踩下"间隔判干净/脏（踩太早）/干（踩太晚），踏板状态可视化+净脏干色点，三种难度窗口，成绩入仪表盘 |
| 🪶 颤音速度 | ✅ | 练颤音（两个相邻音快速来回交替 C–D–C–D…）：选下方音+音程（半音/全音/小三度），又快又匀地交替弹两音，引擎测颤音速度（次/秒）、均匀度（IOI变异系数）、交替正确性（弹错音/没交替会扣分），速度计可视化+双键模拟，**88 键键盘标出两个颤音键（蓝"下"/橙"上"，每击闪光）**，成绩入仪表盘 |
| 🎵 装饰音 | ✅ | 练三种装饰音：倚音（小音+主音，2音）、波音（主-辅-主，3音）、回音（上辅-主-下辅-主，4音）；按目标音序列逐音匹配（弹错音不推进且扣分），引擎测干脆度（装饰音间隔越短越好）+ 均匀度，目标序列可视化逐音点亮 + 一键模拟弹奏，成绩入仪表盘 |
| 🎯 音程大跳 | ✅ | 练"大跳"（相邻音相距八度以上）：生成一串相邻间隔≥设定跳度的目标音，依次"一次弹准"才满分，弹错（摸索）不推进且扣准确度；可选音域（2/3/5 八度）+ 最小跳度（五度/八度/十度）+ 目标数，目标序列点亮/抖动反馈 + 一键模拟，**88 键键盘画出每一跳（蓝"从"→黄"到"，直观看到跳多远）**，成绩入仪表盘 |
| 🔝 旋律突出 | ✅ | 练"声部平衡/voicing"：弹和弦时让旋律声部（最高音，或选最低音）比内声部更响；每轮同时按一个和弦，引擎比较目标声部力度与其余音的最大力度之差，达到设定余量即满分，被埋没（更轻）给 0；可选目标声部/力度余量/和弦数，每和弦色块+力度显示+两种模拟，**88 键键盘显示和弦键位（金"响"=目标声部、蓝色内声部带力度标）**，成绩入仪表盘 |
| 🎚️ 力度渐变 | ✅ | 练 crescendo/decrescendo：连续弹一串音（5/8/12 个），力度整体平滑渐强或渐弱；引擎按方向正确度（相邻力度差符号对不对，权重 50%）+ 平滑度（与首尾线性斜坡的平均偏差，30%）+ 力度跨度（首尾差到设定值封顶，20%）综合评分，SVG 实时画力度曲线（绿/红点标方向对错、虚线为理想斜坡），两种模拟，成绩入仪表盘 |
| 🚀 速度渐变 | ✅ | 练 accelerando/ritardando（含 rubato 收放）：连续敲一串音（6/9/13 下，任意键或空格），速度整体平滑渐快或渐慢；引擎测每两下的间隔 IOI，按方向正确度（间隔变小/变大对不对，50%）+ 平滑度（IOI 与理想斜坡的相对偏差，30%）+ 变速幅度（首尾速度比，20%）综合评分，SVG 实时画 BPM 曲线（绿/红点标方向对错、虚线为理想斜坡），两种模拟，成绩入仪表盘 |
| 🥁 复节奏 | ✅ | 练 polyrhythm（2:3 / 3:2 / 3:4 / 4:3）：两声部在同一周期里平分成不同份数，先听一遍预览再跟着两条轨道敲——连琴时中央 C 以下=左手声部 A、及以上=右手声部 B，没连琴用按钮或键盘 F/J；引擎为两声部各建理想落点网格，按声部把每次敲击匹配到最近未命中落点判 完美/良好/漏/多，综合分=命中率 70%+时间精度 30%，双轨道实时高亮命中、模拟一遍，成绩入仪表盘 |
| 💧 颗粒性 | ✅ | 练 evenness（颗粒感）：连弹一串跑动音（8/12/16 个），让每个音的力度与每两音的间隔都尽量均匀；引擎用变异系数 CV（标准差÷均值）量化离散度，力度均匀分+时值均匀分按可调权重综合（默认各 50%），双排柱状图按相对均值偏差着色（绿=齐、橙=偏），连琴练力度+时值、按钮/空格只练时值，模拟一遍，成绩入仪表盘。与"力度控制"（打目标档）、"节拍稳定度"（跟匀速拍）互补 |
| 🖐️ 手指独立性 | ✅ | 练 finger independence：用部分手指按住几个键不放，同时用其他手指反复敲移动音型（5 套预设，如按 C+E 动 G、按 C+E+G 动 B↔D），可选重复 2/3/4 遍；引擎边记 note-on/off，每敲一个移动音就快照"该按住的音是否都还按着"，统计独立保持率 + 音型正确率（顺序对不对），综合分=独立保持 60%+音型 40%，并计滑脱次数（按住音在收尾前被抬起）；按住键/移动键实时高亮，**88 键键盘标注键位（琥珀"按"=按住键、蓝"移"=移动音型，敲击/滑脱实时闪烁）**，需连琴检测按住与松开、模拟一遍演示评分，成绩入仪表盘 |
| 🎹 音阶八度跨度 | ✅ | 练 scale span：把音阶连续跑过 2~3 个八度（上行/上下行，任意调+8 种音阶），重点不只是音对不对——综合分=音符正确 50%+速度均匀 30%（整串 IOI 的变异系数）+穿指衔接 20%（跨八度根音处间隔是否明显慢于中位=卡顿）；琴键条按八度铺开、穿指点标橙边、跟弹时实时高亮命中/错音，需连琴依次弹、模拟一遍演示评分，成绩入仪表盘。与"音阶练习"（只判音准/进度）互补 |
| 👂 节奏听写 | ✅ | 练 rhythm dictation（耳朵）：先<b>听</b>一段节奏（屏幕<b>不显示</b>长短），再在<b>任意一个键</b>上把它<b>敲回来</b>；评分<b>与速度无关</b>——把你敲出的相邻间隔（IOI）整体缩放到与目标同样的总时长后，逐间隔比相对误差，只看<b>长短比例</b>对不对（短短长 vs 长短短），所以敲快敲慢都行。综合分=节奏比例准确度 × 敲击个数匹配度（多敲/少敲打折）；3 档难度（八分/四分→加附点四分→加十六分/附点八分），听完用 Web Audio 咔哒声播放、敲回来时点亮圆点、打分后用对比条画"目标 vs 你的"长短，需连琴敲、模拟一遍演示评分，成绩入仪表盘。与"节奏跟拍"（看着谱跟节拍器、判绝对落点）不同，这里全凭听 |
| 🎼 移调视奏 | ✅ | 练 sight transposition（看谱移调）：屏幕给一段<b>原调（C）</b>小旋律（6 首唱名片段：上下行五音/主和弦琶音/小星星/欢乐颂/玛丽小羊/下行四音）+ 一个<b>目标调</b>（7 个调），你要把同一段旋律<b>移到目标调</b>弹出来——起音落目标音、其余保持一样音程。综合分=移调后音级正确率（忽略八度）；另算<b>旋律形状</b>（音程序列）正确率，用来识别"旋律对了但调没移对"并给提示；可🔊听原调旋律、👁看答案、🎲模拟一遍，需连琴弹，成绩入仪表盘。和"移调器"（整体升降键盘）、"视奏闪卡"（照谱原样弹）、"旋律听写"（凭听复奏原音高）都不同 |
| 🎹 和弦转位听辨 | ✅ | 练<b>和弦转位</b>听辨：随机播放一个三和弦（大/小三和弦，根音范围可调），你凭听判断它是<b>原位/第一转位/第二转位</b>（多选按钮答题，纯耳朵练习无需连琴）。原位=两个三度叠起来；第一转位=最上方出现纯四度；第二转位=最下方出现纯四度。答完🔊揭晓和弦音名（最低音高亮为低音），统计正确率/连击/最佳，成绩入仪表盘。和"和弦构建"（按名弹和弦、不分转位）、"和弦进行"、"和弦识别"都不同——这里专练<b>转位的听觉辨识</b> |
| 🎼 调号识别 | ✅ | 练<b>调号</b>读谱：屏幕显示一个调号（1~7 个升号 ♯ 或降号 ♭，可限定 ±2/±4/±7），你判断它是哪个<b>大调/小调</b>（多选按钮，纯乐理无需连琴）。基于<b>五度圈</b>：升号顺序 F C G D A E B、降号顺序 B E A D G C F。诀窍——升号调看<b>最后一个升号上方半音</b>是大调主音；降号调看<b>倒数第二个降号</b>是大调主音（仅 1 降号固定 F 大调）；小调再下小三度取关系调。答完显示诀窍提示并🔊播放该调音阶。和"音阶练习"（弹音阶）、"和弦识别"都不同——这里专练<b>看调号秒认调名</b>
- 音程构建用 `interval-build.js`（纯逻辑，48 单元测试）：`INTERVALS` 列出 12 个音程（m2..P8 对应半音 1..12，含名称/简写/半音数）、`DIRECTIONS` 上行(+1)/下行(-1)。`intervalBySemitones(st)`/`intervalById(id)`/`noteName(midi)`/`targetMidi(root,st,dir)` 是纯工具函数。`IntervalBuildGame({rng,intervals,directions,rootMin,rootMax})`：`next()` 随机出题（根音范围按方向+音程收紧，保证目标音落在 0..127）、`check(playedMidi)` 须精确等于目标音才判对、`score/streak/best/accuracy/reset()`，`onNew`/`onResult` 回调。UI 显示"从 X 根音 向上/下 某音程"，🔊 播根音参考，note-on 接 `ivbOnNote` 钩子判分，弹对 700ms 后自动出下一题、弹错提示差几个半音，成绩入仪表盘。和"听音训练"相反——练<b>反向构建</b>能力 |
- 调式识别用 `mode-id.js`（纯逻辑，146 单元测试）：`MODES` 列出 7 个教会调式（各含 `offsets[7]` 相对主音半音、`degree` 在大调中的级数、`hint` 特征提示）、`scaleMidi(root,modeId)` 生成 8 音音阶（含八度）、`stepPattern(modeId)` 返回全/半音步进指纹（如 Ionian=[2,2,1,2,2,2,1]、Dorian 是回文）。`ModeIdGame({rng,modes,rootMin,rootMax,choiceCount})`：`next()` 随机出题并构建打乱的多选干扰项、`notes()`/`choices()`/`check(answerId)`/`score/streak/best/accuracy/reset()`，`onNew`/`onResult` 回调。这是<b>纯听辨多选</b>（像调号识别/和弦转位），用 `playTone` 播音阶、点选项判分，无 note-on 钩子。答完显示调式特征提示，可🔊再听，成绩入仪表盘 |
- 唱名听辨用 `solfege.js`（纯逻辑，196 单元测试）：`SCALES`（大调/小调的半音步进 + 可动唱名，小调用 Me/Le/Te）、`DEGREES` 七级功能名+诀窍、`degreeMidi(tonic,degree,scaleType)`/`tonicTriad(tonic,scaleType)`/`syllable(degree,scaleType)` 是纯工具函数。`SolfegeGame({rng,scaleType,degrees,tonicMin,tonicMax,choiceCount})`：`next()` 随机选主音+音级（启用音级里取干扰项打乱）、`triad()`/`target()`/`tonic()`/`choices()`/`check(answerDegree)`/`score/streak/best/accuracy/reset()`，`onNew`/`onResult` 回调。<b>纯听辨多选</b>，UI 先用 `playTone` 播主和弦建立调性、隔 750ms 再播目标音，点选项判分（无 note-on 钩子），主和弦与目标音可分别重听，成绩入仪表盘 |
- 和弦性质听辨用 `chord-quality.js`（纯逻辑，185 单元测试）：`QUALITIES` 列出 9 种和弦性质（4 三和弦 major/minor/aug/dim + 5 七和弦 dom7/maj7/min7/m7b5/dim7，各含 `intervals[]` 相对根音半音、`symbol` 和弦记号、`family` 三/七和弦、`hint` 色彩诀窍）、`chordMidi(root,qualityId)` 构造和弦音。`ChordQualityGame({rng,qualities,rootMin,rootMax,choiceCount})`：`next()` 随机出题并构建打乱的多选干扰项、`notes()`/`choices()`/`root()`/`check(answerId)`/`score/streak/best/accuracy/reset()`，`onNew`/`onResult` 回调。<b>纯听辨多选</b>（像调式识别/转位听辨），UI 用 `playTone` 按选定方式播整块和弦/琶音、点选项判分（无 note-on 钩子），可🔊再听，成绩入仪表盘
- 和声进行听辨用 `progression-ear.js`（纯逻辑，337 单元测试）：`DEGREES` 列出大调 7 个自然音级三和弦（罗马数字 I/ii/iii/IV/V/vi/vii°，各含 `quality`/`name`/`hint` 功能诀窍）、`PROGRESSIONS` 含 8 个常用进行（流行 I-V-vi-IV、doo-wop、卡农、ii-V-I、变格、正格、忧伤、摇滚）、`chordMidi(tonicMidi,degree)` 按调内三度叠置构造自然音级三和弦（含八度回绕）。`ProgressionEarGame({rng,progressions,tonicMin,tonicMax,choiceCount})`：`next()` 随机选进行+主音并构建整段和弦（第 1 个 I 为锚点）、`progressionNotes()`/`chords()`/`currentChord()`/`choices()`/`check(answerDegree)` 逐个和弦判分推进、`isComplete()`/`score/streak/best/accuracy/reset()`，`onNew`/`onResult`/`onComplete` 回调。<b>纯听辨多选</b>，UI 用 `playTone` 顺序播整段、高亮当前待辨和弦、点罗马数字选项判分，成绩入仪表盘
- **通用全幅虚拟钢琴** `piano-keyboard.js`（纯布局逻辑 46 单元测试）：一个可在任意模块复用的 88 键（A0–C8）钢琴组件。纯函数 `buildLayout(first,last,opts)` 算出白/黑键的绝对几何位置（白键等宽并排、黑键居中压缝），`isBlack`/`noteName`/`whiteCount` 等辅助；DOM 类 `PianoKeyboard(container,{labels,onNoteOn,onNoteOff})` 渲染可点击键盘，方法 `highlight/highlightMany(把答案音画在真实键位上，带炫彩描边+序号徽章)`、`flash(演示跟随点亮)`、`press/release(回显外部 MIDI)`、`scrollToShow(自动滚动居中)`、`clear`。按下/点亮带渐变发光动画。已接入：**音色浏览器**（点键发真实 MIDI 试听当前音色，切音色后不用去琴上就能判断对错）、**音阶练习**（整条音阶高亮+▶标下一个该弹的键）、**旋律听写**（点键输入、播放跟随点亮、放弃看答案时按顺序高亮）、**音程听辨/唱名听辨/和弦性质听辨/和声进行听辨**（答完把正确音画在键盘上）、**视奏闪卡**（没接 MIDI 也能点键作答，答错把正确音高亮）、**和弦练习**（挑战开始即把目标和弦该按的键高亮）、**和弦转位听辨**（答完画出三和弦音，低音特别标色）、**音程构建**（弹错/弹对都画出根音+目标音）、**调号识别/调式识别**（答完把该调/调式音阶带级数 1-7 画在键上）、**移调视奏**（看答案/完成把移调后目标音按序画出）、**移调器**（点键发真实移调 MIDI 直接听效果）、**和弦进行**（当前该弹的和弦实时高亮）、**音阶指法提示**（整条音阶画在 88 键上、每键标手指号、▶ 标当前该弹的键、红色标穿指/跨指点）、**曲谱跟弹**（Synthesia 式落音符到对应键、判定时刻高亮该弹的键）、**乐句视奏**（看答案把整句按序号画在 88 键上、弹错高亮该弹的音）、**和弦视奏**（看答案把整组叠置和弦该按的键画在 88 键上、按对变绿/按错变红）、**五度圈**（当前调音阶高亮+主音金色、点和弦/终止式点亮试听三和弦）。这样即使看不懂题目，看答案在 88 键上的位置就知道该怎么弹 |
| 🖐️ 音阶指法提示 | ✅ | 学标准钢琴<b>音阶指法</b>：屏幕给一个八度音阶（9 个调：C/G/D/A/E/F 大调 + A/E/D 自然小调），每个音上方标注<b>该用几号手指</b>（右手 1=拇指…5=小指，左手相反），可切左/右手、上行/上下行。<b>跟着弹</b>——弹对当前音就高亮下一个，红框标出<b>穿指/跨指点</b>（右手拇指穿过、左手手指跨过）。诀窍：C/G/D/A/E 大调右手都是 <b>1 2 3 1 2 3 4 5</b>、F 大调例外 <b>1 2 3 4 1 2 3 4</b>；左手都是 <b>5 4 3 2 1 3 2 1</b>。可🔊听一遍，需连琴弹，成绩入仪表盘。和"音阶练习"（练音准/速度）、"音阶八度跨度"（练跨多个八度）都不同——这里专练<b>正确指法与穿指动作</b> |
| 🎯 音程构建 | ✅ | "听音训练"的<b>反向能力</b>：屏幕给一个<b>根音</b>+一个<b>音程名</b>（如"从 C4 往上弹纯五度"），你在键盘上<b>弹出目标音</b>。可选基础 6 种（M2/m3/M3/P4/P5/P8）或全部 12 种音程，方向可选向上/向下/双向。🔊 先听根音找位置，弹对自动判分并播下一题，弹错显示差几个半音。练即兴/移调/和声的核心手上功夫，需连琴弹，成绩入仪表盘。和"听音训练"（听两音辨音程）相反——这里练<b>知道音程名就在键盘上秒构建</b> |
| 🎶 调式识别 | ✅ | 🔊 听一段<b>调式音阶</b>（7 个教会调式 Ionian/Dorian/Phrygian/Lydian/Mixolydian/Aeolian/Locrian 之一），从多个选项中<b>辨认是哪个调式</b>。可自选调式范围与选项数量（3/4/7 选 1）。答完显示调式特征提示（如利底亚=升四度、混合利底亚=降七度、弗里几亚=降二度），可🔊再听。无需连琴（纯听辨多选），成绩入仪表盘。补足"音阶训练"（练手）和"听音训练"（辨音程）之外的<b>调式色彩听辨</b>能力 |
| 🎵 唱名听辨 | ✅ | 视唱练耳的<b>地基</b>：先🔊听一个<b>主和弦</b>建立调性，再听一个音，判断它是音阶里的<b>第几级</b>（唱名 Do Re Mi Fa Sol La Ti / 1-7）。支持大调/小调（小调用 Me/Le/Te 唱名）、自选音级范围与选项数量（3/4/7 选 1），可分别🔊再听主和弦或目标音。答完显示该音级的功能名与诀窍（如 Ti→Do、Fa→Mi）。无需连琴（纯听辨多选），成绩入仪表盘。和"音程听辨"（听两音距离、不依赖调性）不同——这里练<b>调性感/相对音高</b>，是即兴扒谱视唱的核心 |
| 🎹 和弦性质听辨 | ✅ | 🔊 听一个<b>和弦</b>，辨认它的<b>性质/类型</b>：大三/小三/增三/减三（三和弦）或属七/大七/小七/半减七/减七（七和弦）。可自选和弦范围、播放方式（仅整块/仅琶音/整块+琶音）与选项数量（3/4/9 选 1）。答完显示该和弦的色彩诀窍（如增三悬浮对称、减七极度紧张）。无需连琴（纯听辨多选），成绩入仪表盘。和"和弦转位"（练同一和弦的不同排列）、"和弦练习"（弹出和弦名）都不同——这里练<b>和弦色彩/类型听觉</b>，是和声听辨的地基 |
| 🎶 和声进行听辨 | ✅ | 🔊 听一段<b>大调和弦进行</b>，逐个辨认每个和弦的<b>罗马数字级数</b>（I/ii/iii/IV/V/vi/vii°）。第 1 个和弦固定 I 作锚点，之后辨每个和弦的功能/走向。可自选进行范围（流行/doo-wop/卡农/ii-V-I/变格/正格/忧伤/摇滚 8 种）与选项数量（3/4/7 选 1）。答完显示该级功能诀窍（如 V 强烈想回 I、vii° 极不稳定）。无需连琴（纯听辨多选），成绩入仪表盘。和"和弦进行"（看级数弹出和弦）不同——这里练<b>和声功能/进行走向听觉</b>，是扒和弦/即兴/编配的核心 |
| 🔚 终止式辨认 | ✅ | 🔊 听一个<b>两个和弦</b>的<b>终止式</b>（乐句的和声落点），辨认它属于哪一类：<b>正格 V→I</b>（最有结束感像句号）、<b>变格 IV→I</b>（柔和庄重的"阿门"）、<b>半终止 ?→V</b>（停在属和弦上悬而未决像逗号）、<b>阻碍 V→vi</b>（本想回主却走到 vi 制造意外）。可自选终止式范围与选项数量（2/3/4 选 1），答完显示功能诀窍并把<b>两个和弦画在 88 键上</b>（第①个蓝/第②个金、标罗马数字）。无需连琴（纯听辨多选），成绩入仪表盘。是听辨乐句结构、即兴收束、扒歌分段的核心能力 |
| 🔤 键盘音名认知 | ✅ | 最基础的<b>认键基本功</b>：把<b>音名</b>（C4=中央 C、F#3…）和 88 键上的<b>实际键位</b>对应起来——看懂任何练习答案的前提。两种练法：<b>看音名找键</b>（屏幕给音名 → 在键盘上点对应的键，答完绿色标出正确键、点错红色标出）、<b>看键认音名</b>（键盘点亮一个键 → 从 4 个选项里选出它叫什么）。可选音域（中音区 C3–C5 / 宽 C2–C6 / 全 88 键）、<b>只白键</b>（新手友好）、音名<b>带不带八度数字</b>（带八度时只认同一个键、不带时任意八度同名键都算对）。无需连琴，成绩入仪表盘 |
| 🎼 五线谱识谱卡 | ✅ | 看懂五线谱的<b>第一步</b>：屏幕在五线谱上画一个音符 → 你<b>说出它的音名</b>（"看谱→音名→键位"读谱链的中间环，和"键盘音名认知"配合从此读谱不靠数线）。两种练法：<b>看谱选音名</b>（从 4 选项选）、<b>看谱点键</b>（在 88 键上点出它）。答完显示<b>记忆口诀</b>（高音谱号线 EGBDF·间 FACE / 低音谱号线 GBDFA·间 ACEG）并把音画在键盘上。可选谱号（高音 𝄞 / 低音 𝄢 / 大谱表随机）、音名带不带八度。和"视奏闪卡"（必须真弹）不同——这里纯认读，零基础也能上手。无需连琴，成绩入仪表盘 |
| 🎼 乐句视奏 | ✅ | 从识谱迈向<b>流畅演奏</b>的关键一步：屏幕给一句<b>标准五线谱</b>记谱的短旋律（带<b>节奏</b>符头/符干/符尾旗/附点 + <b>调号</b>升降号 + 4/4 拍号 + 小节线），你<b>照着谱、按自己的节奏从左到右逐音弹出来</b>。弹对的音<b>变绿</b>、当前该弹的音<b>金色高亮</b>并自动滚动居中，弹错<b>闪红不前进</b>并提示该弹音名——整句一遍弹对才计满分、累计连击。和"视奏闪卡"（只一个音）、"识谱卡"（只说音名）、"旋律听写"（靠耳朵）、"曲谱跟弹"（音符下落+计时）都不同：<b>这里脱离听觉与下落提示，纯靠读谱</b>。可选调（升降号 ≤2 的 8 个调）、乐句长度（1–3 小节）、节奏难度（入门四分/二分 → 进阶加八分 → 挑战加附点）、是否忽略八度。卡住可 👂 <b>试听</b>一遍或 🏳 <b>看答案</b>（把整句按序号画在键盘上）。没接 MIDI 也能点屏幕琴键作答，成绩入仪表盘 |
| 🎹 和弦视奏 | ✅ | 真实钢琴谱里大量的音是<b>竖向叠在一起的和弦</b>——看懂一摞叠置三度音符、一眼认出该<b>同时按哪几个键</b>，是从单音识谱走向弹真正乐曲的核心技能。屏幕在五线谱上画一个<b>叠置和弦</b>（三和弦/七和弦，可带<b>转位</b>、带<b>调号</b>，相邻二度音符自动错位、共用符干），你在键盘上<b>把整组音同时按下</b>即过关：按对的键/符头<b>变绿</b>、按错<b>闪红</b>并提示松开，集齐即判分并报出<b>和弦名</b>（如「C 大三和弦」「A 小三和弦」），转位模式还会校验<b>最低音</b>是否弹对。和"和弦练习"（给<b>和弦名</b>让你弹，不读谱）、"和弦性质听辨"（靠<b>耳朵</b>，谱面不显示）、"五线谱识谱卡/视奏闪卡"（只<b>单音</b>）、"乐句视奏"（<b>横向单音旋律</b>）都不同——<b>这里读纵向叠置和弦、整组同时按</b>。可选调（8 个）、和弦类型（三/七/混合）、是否含转位、是否忽略八度。没接 MIDI 时可<b>逐键点选</b>（再点取消），集齐自动判定，卡住可 🏳 看答案。成绩入仪表盘 |
| 🥁 节奏视奏 | ✅ | 视奏的另一半是<b>节奏</b>——光会认音名/音高还不够，得能<b>看着节奏谱按准拍子</b>。屏幕画一行<b>单声部节奏谱</b>（随机生成各小节时值组合：四分/八分/十六分/二分/附点 + 休止符 + 符梁/符干/附点，每小节恰好填满拍号），点「预备—开始」后<b>节拍器先给一小节预备拍</b>，然后你跟着谱子<b>按任意键或点大 TAP 按钮击打</b>，光标随拍走。判分给出<b>完美/良好/漏击/多击</b>计数 + <b>抢拍/拖拍倾向</b> + 复用「曲谱跟弹」的<b>落点时间对比图</b>（中线为准点、绿带±完美、青带±良好、点在上=抢拍、下=拖拍）。和"乐句/和弦视奏"（读音高）、"曲谱跟弹"（音符下落自带计时提示）都不同——<b>这里只读节奏、纯靠内心拍感击打</b>。可选拍号（4/4·3/4）、小节数（1/2/4）、难度、BPM。无需 MIDI，点屏幕 TAP 键即可，成绩入仪表盘 |
| 🪗 伴奏音型 | ✅ | 会按和弦 ≠ 会伴奏——真实弹琴时左手不是干巴巴按柱式，而是用各种<b>伴奏型</b>把同一串和弦弹出律动。本模块把所选调上的<b>和弦进行</b>（复用"和弦进行练习"的万能流行/卡农/ii–V–I/12 小节布鲁斯等级数库）用<b>五种伴奏型</b>展开成一串"该弹的音"步骤：<b>柱式和弦</b>（整三和弦齐按）、<b>阿尔贝蒂低音</b>（低-高-中-高分解，C：C-G-E-G，莫扎特奏鸣曲经典）、<b>华尔兹</b>（蓬-恰-恰：低音+两下和弦，3/4）、<b>分解琶音</b>（根-三-五-八上行）、<b>行进低音</b>（根-五-六-五布吉低音，布鲁斯/摇滚）。屏幕上方<b>和弦进行卡片</b>标当前和弦，中间<b>伴奏型步骤条</b>逐拍显示该弹的角色（低/高/中/根/三/五/八/六）与音名，88 键上把<b>当前该弹的音</b>带角色标签高亮，你<b>照位置按顺序弹出</b>即逐步推进（步进式判分，不靠下落计时），连续弹对累计 <b>Combo</b>，走完整条按正确率给 ★ 评星。和"和弦进行练习"（只按出和弦本身、不讲伴奏型）、"曲谱跟弹"（跟现成曲子）都不同——<b>这里专练把和弦变成伴奏的左手织体</b>。没连琴可点 🔊 <b>试听整条</b>（按 BPM 播放音型+视觉走光）或 🎹 <b>替我弹当前</b>逐步演示。可选调（8 个）、进行（7 条）、伴奏型（5 种）、速度（60/90/120）、忽略八度/要弹准八度，成绩入仪表盘 |
| 🌈 和弦色彩板 | ✅ | 入门<b>声光 + 和声</b>启蒙：在琴上弹下任意和弦（≥3 键，或点快捷和弦/直接点琴键搭），屏幕舞台与 88 键立刻<b>染上对应的颜色光</b>，并报出<b>名字 / 性格情绪 / 在调里的功能</b>。配色按和声品质区分——<b>大三暖橙☀️、小三冷蓝🌙、属七亮黄"想回家"🏃、减和弦警示红"紧张"😣、增三梦幻紫🌀、大七爵士暖🍷</b>等；属七/减/增/半减/减七等张力和弦会<b>闪烁</b>提醒。设了<b>调</b>（C/G/D/F/A 大调、a/e/d 小调）后还会算出它的<b>罗马级数</b>（I·ii·V7·vii°…）与<b>功能名</b>（主/下属/属），弹到"属"功能时同时把<b>主和弦（家🏠）</b>淡淡标在键上，提示你可以"解决回家"。另含 <b>🎯 认色挑战</b>：给出品质名（大三/小七/属七…）让你弹出对应和弦，8 关计分入仪表盘。复用 chord-detect 识别，把抽象和声变成看得见的光、听得懂的情绪 |
| 🎆 自由演奏灯光秀 | ✅ | 不绑定曲目的<b>纯声光娱乐</b>模式：随便弹，你弹的<b>每个音</b>都在深色舞台对应位置喷出<b>力度感应</b>的火花束 + 升起一道<b>光柱</b>，并点亮 88 键上的实际琴键——弹得越用力，火花越多越暖、光柱越高（轻弹冷蓝小火花 → 重弹暖红大爆发）。连续快弹累积<b>连击</b>，到 5/10/15… 触发 🔥 大爆发飘字。四种<b>配色主题</b>：🔥力度热力（按触键力度配色）/🌈音高彩虹（低音红→高音紫）/🌌极光/💖霓虹。可开关 ✨击中特效、🔊点键发声，并实时统计弹了几个音 / 最高连击。接 CA99 时直接把真实触键力度画成一场灯光秀，没连琴点屏幕琴键也行——很适合给小朋友玩。纯逻辑 `light-show.js`（颜色/火花参数/光柱高度/连击计数，16 单元测试） |
| 🪄 魔法即兴沙盒 | ✅ | <b>零失败自由创作</b>沙盒（Magic Jam）：把键盘「锁」到<b>五声音阶</b>——<b>怎么弹都好听</b>，无评分、无对错、无 game-over，专为<b>「爱音乐讨厌练琴」的孩子</b>把「练琴」重新变回「玩音乐」。88 键上的<b>五声「魔法键」按彩虹高亮</b>（脚手架），接 CA99 弹<b>任何</b>键都会<b>就近吸附</b>到最近的好听音上（`snapToScale`，所以真琴乱弹也只会响出好音）。下方一排<b>大彩色音垫</b>触屏友好，点一下即发声。5 个友好调（C/G/F/D/A）× 2 种心情（☀️ 大调五声 / 🌙 小调五声）随选随换。可叠加 🥁 <b>律动伴奏</b>（底鼓/军鼓/踩镲 + 根·五度贝斯，速度 60-140 可调）——<b>仅浏览器 Web Audio 合成，绝不发往 CA99</b>（真琴留给孩子弹）；切走模块自动停。弹音迸发<b>力度感应火花 + 光柱</b>，连弹累积 🔥 连击里程碑。每弹 24 个音记一次练习喂连胜/今日目标/仪表盘（自由玩耍也算练琴）。调/心情/速度持久化。纯逻辑 `magic-jam.js`（五声音级集合/就近吸附/音垫生成/伴奏型，12 单元测试） |
| 🗓️ 每周挑战 | ✅ | <b>限时主题挑战赛季</b>（Weekly Quest）——每<b>自然周（周一→周日）</b>轮换一个<b>主题</b>（🪄五声创作周 / 🥁节奏周 / 👀识谱周 / 👂听辨周 / 🎵和声周 / 🚀街机挑战周），主题下有 2-3 个<b>小任务</b>（在指定模块练够 N 次）。一周内完成<b>全部任务</b>即可领取一枚<b>专属赛季徽章</b> 🏅，集成「徽章墙」。制造「<b>这周不玩就过期</b>」的温和回访动机——但错过<b>只是少一枚徽章，从不扣分、不惩罚</b>。任务进度<b>纯靠 `recordPractice` 自动计数</b>（每次某模块记一次练习，就给本周引用它的未完成任务 +1，封顶 goal），无需新埋点；任务卡有「去玩 →」按钮直接 `switchModule` 跳到对应模块。页面含<b>主题横幅 + 总进度条 + 本周倒计时</b>、逐任务进度条、完成时<b>领取按钮</b>（触发 `victoryLightShow` 全屏庆祝）+ 徽章陈列架。周一换周自动清零进度、换新主题。纯逻辑 `weekly-quest.js`（周一归位/周键/主题轮换/周倒计时/任务计数，14 单元测试） |
| 👻 幽灵竞速 | ✅ | <b>幽灵竞速</b>（Ghost Race）——和<b>过去最快的自己</b>赛跑！选一段乐句（C 大调音阶 / C 大三琶音 / C 大调上下行 / G 大调音阶）按顺序弹对，引擎给你<b>计时</b>；屏幕上的 🏃 你 与 👻 幽灵在赛道上并行——幽灵按<b>上次最快一跑</b>的速度前进，<b>比幽灵先冲线 = 打败昨天的我</b>，刷新个人纪录。完全契合 Lily<b>成长思维脆弱点</b>：<b>不和别人比、只超越自己</b>，弹错只是从头来（<b>不惩罚、无 game-over</b>）。奖牌按<b>首跑 / 破纪录 / 险胜 / 再试试</b>分级，鼓励向措辞。canvas 双跑道动画（你 vs 幽灵）+ 个人最佳/上次用时/本次计时三栏仪表 + 乐句音名条；八度无关，真琴/屏幕键盘双输入。纯逻辑 `ghost-race.js`（4 赛道/幽灵进度插值/projectedMs 预测/领先判定/奖牌分级，16 单元测试） |
| 🥁 节奏跳跳 | ✅ | <b>节奏跳跳</b>（Rhythm Jump，太鼓达人式）——节奏方块顺轨道下落，落到<b>击拍线</b>那一刻<b>按任意键</b>（或点大鼓按钮）拍中。<b>只判节奏、不判音高</b>——不用找对键，门槛比接音水滴更低，是最轻松的<b>热身 / 低能量日</b>游戏。判定分<b>完美 / 不错 / 漏拍</b>（±55 / ±130ms 窗口）；空挥不扣连击、漏拍只断连击——<b>永远不会输</b>。5 种节奏型按难度分级：四分音符 🌱 / 华尔兹 3/4 🌱 / 八分音符 🌿 / 混合节奏 🌿 / 切分音 🔥；可调 BPM（60–130）+ 🔊 节奏向导（方块到线给提示音）+ 4 拍预备拍 + 重音方块（● 金 / ♪ 蓝）。canvas 下落轨道 + 击拍线脉冲 + 漂浮判定字；命中率评星 1–3 ★，平均误差 ms 反馈。真琴/屏幕键盘/大鼓按钮三路输入。纯逻辑 `rhythm-jump.js`（5 节奏型/拍位时刻表/判定窗口/空挥保护/评星，16 单元测试） |
| 👯 双人对战 | ✅ | <b>双人对战</b>（Family Duel，进阶）——和 <b>👨 爸爸 / 👩 妈妈 / 🧒 朋友</b><b>轮流上场</b>，把孤独练琴变成<b>亲子互动</b>（命中家庭「连接」核心价值）。每回合照着<b>高亮的音</b>按顺序弹（C 大调随机序列，可选 🌱3 / 🌿4 / 🔥5 音），<b>弹对一个得 1 分、弹错不扣分</b>；几回合（2–4 可选）下来比<b>总分</b>。换人时显示「轮到 X」准备横幅，<b>结束时两个人都祝贺</b>——赢家正向鼓励、平局有专属文案、永不羞辱输家，并强调你们的<b>合作总分</b> 🎉 + 撒花 + 灯光秀。八度无关；真琴（`familyDuelOnNote` 钩子）/ 屏幕键盘双输入；切走模块自动停。纯逻辑 `family-duel.js`（turnOrder/startTurn/press/endTurn/result/teamTotal，15 单元测试） |
| 🎨 音画涂鸦 | ✅ | <b>音画涂鸦</b>（Sound Painting，初级 / 自由创作）——<b>没有对错、没有分数、没有 game-over</b>。你弹的<b>每一个音</b>都会在画布上点亮一笔径向渐变光斑：<b>音高 → 颜色</b>（12 色彩轮，低音偏红 / 高音偏紫）+ <b>横向位置</b>，<b>时间 → 纵向位置</b>（画面从上往下慢慢填满），<b>力度 → 光斑大小 + 透明度</b>。随便弹就能画出一幅独一无二的画 🌈，对抗 Lily「爱音乐讨厌重复」——把练琴变回玩音乐。4 种背景（🌌 星夜 / 🌅 朝霞 / 🌊 深海 / 📜 画纸）；🗑️ 新画布 / 💾 保存 PNG / 📤 分享（Web Share，喂家长认可）。真琴（`soundPaintOnNote` 钩子）/ 屏幕键盘双输入。纯逻辑 `sound-paint.js`（音→笔触映射 + SoundPainting 收集/配色/统计，13 单元测试） |
| 🐣 养成小伙伴 | ✅ | <b>养成小伙伴</b>（Practice Pet，初级 / 养成）——一只<b>随累计练习成长、进化</b>的音乐宠物，成长值<b>直接复用经验系统</b>（与等级同源），叠加「养成 + 拥有感」让孩子<b>为了它</b>而练。8 个进化阶段：🥚 音乐蛋 → 🐣 破壳雏鸟 → 🐤 唱歌小鸡 → 🐦 旋律鸟 → 🦜 七彩鹦鹉 → 🦚 音乐孔雀 → 🦢 天鹅歌者 → 🦅 传奇音乐鹰。<b>鼓励向：只会越长越棒，永不死亡 / 退化 / 惩罚</b>；没练时只是「有点想你」（温和）。进化是惊喜——跨过门槛 🎉 全屏庆祝 + toast。蹦跳头像 + 进度条 + 进化之路画廊（已达 / 当前 / 未解锁剪影）+「去玩音乐」一键跳魔法即兴。挂在 `recordPractice` 链（`petGrowOnUpdate`，无新埋点）。纯逻辑 `pet-grow.js`（8 阶段 / petXp / stageFor / progressToNext / moodFor / petSummary 进化检测，14 单元测试） |
| 🎼 循环作曲台 | ✅ | <b>循环作曲台</b>（Loop Composer，初级 / 自由创作）——把 🪄 魔法即兴<b>升级成「我创作了一首歌」</b>。键盘同样被锁到<b>五声音阶</b>（怎么弹都好听、零失败），但这里你录的每一段都会<b>自动循环</b>播放，然后<b>再叠一层</b>和声、再叠一层低音……几层一叠就是<b>你自己的一首歌</b>。多层叠录（overdub）+ 网格量化（自由 / ♩ 每拍 / ♪ 八分 / ♬ 十六分自动对齐到节拍）+ 每层独立<b>静音 / 清空</b>；canvas 钢琴卷帘按层分色 + 黄色播放头实时滚动。录制带 <b>4 拍预备拍</b>（🥁 浏览器节拍器，<b>绝不发往 CA99</b>，真琴留给孩子）；播放 / 乐句声部全部走<b>独立 Web Audio 合成</b>。弹完一键 💾 导出 <b>MIDI</b>（合并所有层 + 重复 2 遍）或 🎉 存成<b>作品卡 PNG</b>（渐变背景 + 迷你卷帘 + 音符 / 层数统计）给爸爸妈妈看——拥有感 + 成就感最强。真琴（`loopComposerOnNote/OffNote` 钩子）/ 屏幕键盘双输入。纯逻辑 `loop-composer.js`（多层 / 量化 / events 合并 / toMidiFile 复用 recorder.js SMF 原语，14 单元测试） |
| 🔁 循环慢练 | ✅ | <b>AB 循环慢练器</b>（Loop & Slow Trainer，中级 / 写作业工具）——全 app 唯一直接帮上<b>真钢琴课</b>的工具：把老师布置曲子里的<b>难句框出来反复磨</b>。① <b>框选 A→B 小节</b>只循环这一段（区间精练）；② <b>从慢速起步</b>（默认 60%，可选 50–100%），每<b>弹干净一遍自动提速 +10%</b>，一路爬到 <b>100% 原速</b>（🐢→🐇 速度阶梯，弹脏的一遍不提速、留在原速再练）；③ <b>左右手分层</b>——上传双手 .mid 可只练单手、练熟再合手。<b>等待式</b>：弹对当前音才走下一个，<b>容差放行</b>（节奏对、音高差≤2 半音也帮过，不卡死孩子）。配 🥁 <b>浏览器节拍器</b>（拍号自动跟曲子、随速度阶梯变 BPM、可视脉冲，<b>绝不发往 CA99</b>）稳住拍子。真琴（`loopTrainerOnNote/OffNote` 钩子）/ 屏幕键盘双输入；原速干净弹完一段 → 🏆 庆祝。复用 `score-follow.js`（音符解析 / 容差匹配 / 分组 / 手别过滤）+ 纯逻辑 `loop-trainer.js`（SpeedLadder 速度阶梯 / measureWindow 小节窗 / LoopSession 逐组推进与干净遍计数，20 单元测试）。 |
| 🎭 情绪演奏 | ✅ | <b>情绪演奏</b>（Play the Mood，初级 / 自由表达）——抽一张<b>情绪卡</b>（开心☀️ / 神秘🌙 / 暴风雨⛈️ / 平静🌊 / 悲伤💧 / 魔法✨），孩子用<b>力度 + 速度 + 音色</b>自由弹出这种感觉，<b>零对错、零评分、零 game-over</b>。每张卡带<b>情绪提示</b>（建议力度档、快/慢、和弦色彩 chip——复用 dynamics-trainer 力度 + chord-color 情绪色），弹完点「✨ 弹好了，看看」→ <b>永远鼓励向</b>的点评（贴合情绪就夸、不贴合也只温和建议「下次试试更轻/更快」，<b>从不说错</b>）。培养 technique 之外的<b>音乐表现力</b>。真琴（`playMoodOnNote/OffNote` 钩子，按力度生成笔触色）/ 屏幕键盘双输入。纯逻辑 `play-mood.js`（MOODS 6 卡 / 自由演奏力度·时长·音域捕捉 / reflect 永远鼓励，15 单元测试） |
| 🌅 热身例程 | ✅ | <b>每日热身例程</b>（Warm-up Routine，初级 / 例程）——给「不知道今天先练啥」的日子一个<b>零决策入口</b>，像真正的老师一样带着走：<b>① 音阶暖手 → ② 稳住节奏 → ③ 弹首小曲</b>，三步超小、<b>不用自己决定</b>，走完就给今天开了个好头 ☀️。例程<b>按日期确定性生成</b>（同一天刷新不变、换天换一批，复用 daily-goal 的 hashStr）；三步各复用现成件——音阶（dice-warmup 的调性×音型，键盘上逐音弹对校验，<b>八度无关 / 弹错从头来不惩罚</b>）、节奏（metro-kit 拍号 + 温和 BPM，一键 ▶ 跟着拍 2 小节，<b>纯 Web Audio 节拍器绝不发往 CA99</b>，可视脉冲 + 重音橙闪）、小曲（从现成歌曲/游戏模块里挑一个，一键跳过去弹）。走完三步 → 🎉 全屏庆祝 + 打卡，<b>累计天数只增</b>。真琴（`warmupOnNote/OffNote` 钩子，仅音阶步激活时挂上）/ 屏幕键盘双输入。纯逻辑 `warmup-routine.js`（pickRoutine 按日期组三步 / WarmupRoutine 进度+逐音校验+按日打卡累计，16 单元测试） |
| 📊 经验等级 | ✅ | <b>统一经验 / 等级系统</b>（XP & Level）——把所有练习串成<b>一条成长主线</b>。你做的<b>每一次练习</b>（答对题 ×2 / 练习次数 ×10 / 玩过的新玩法 ×30 / 连练天数 ×15 / 解锁成就 ×50）都换算成<b>经验值</b>，攒够就<b>升级</b>、解锁 10 个递进<b>称号</b>（🌱 小芽琴手 → 🎵 音符新手 → … → 👑 传奇演奏家）。升级瞬间触发<b>全屏庆祝</b>（复用 `victoryLightShow`：CA99 真琴亲奏华彩 + 撒花）。页面含<b>大经验进度条</b>（距下一级还差多少）、<b>经验来源明细</b>（每项来源各贡献多少）、<b>称号阶梯</b>（已达/当前/未解锁一目了然）。元进度是最强的长期留存胶水，给孩子「我正在越来越棒」的身份认同。经验<b>纯由已有累计统计派生</b>（`practice-stats` 快照 + 成就数），无需新埋点；纯逻辑 `xp-level.js`（经验加权/升级曲线/称号映射/来源拆解，12 单元测试），任何模块完成都自动喂经验 |
| 🎲 今日推荐曲 | ✅ | <b>破解「不知道弹什么」</b>——从 CA99 内置的 <b>1500+ 首曲库</b>（midi/catalog.json）里，<b>按日期确定性</b>地推一首「今天试试这首」：同一天打开永远是同一首（像每日惊喜 🎁），<b>🎲 换一首</b>则随机跳到另一首。卡片显示曲名 / 作曲者 / 分类（📕课程/🎹钢琴/🎼音乐会/⛪赞美诗/🌙轻音乐/🎧示范）；<b>▶ 试听</b>用浏览器 Web Audio 直接播放前 28 秒旋律（fetch→parseMidi→调度发声，限并发、<b>绝不发往 CA99</b>）+ ⏹ 停止；<b>🎹 去跟弹这首</b>一键把它载入「曲谱跟弹」（派发 `scf:load-path` 事件 → 自动 fetch+解析+切到 score-follow，可分级慢练）。纯逻辑 `daily-song.js`（本地日期键/FNV-1a 哈希/确定性选号/曲名清洗，11 单元测试） |
| 🧠 记忆宫殿 | ✅ | <b>旋律记忆宫殿</b>（渐进 Simon · 真实曲调）——契合 Lily <b>听觉智能 85%</b> 的最强项：挑一首真实曲子（中央C/五指阶梯/小星星/欢乐颂/玛丽的小羊/铃儿响叮当…），钢琴<b>每次只放前几个音</b>，你照着<b>弹回来</b>；弹对了它就在末尾<b>多放一个音</b>——一句一句往脑子里搬，<b>不读谱、纯靠耳朵</b>，最后整首歌就住进你脑子里 🏰。和「旋律回声」不同：那是随机生长的序列，这是用<b>固定的真实旋律</b>逐音累积，揭示到曲尾并复奏成功 → 🏆 <b>整首掌握</b>庆祝。可<b>忽略八度</b>（任意八度都算对）+ <b>容错放行</b>（差一点点也提示通过、不卡住孩子，默认开），可调速度，进度条显示已学到 N/总音数。真琴（`melodyPalaceOnNote` 钩子）/ 屏幕键盘双输入。纯逻辑 `melody-palace.js`（音高提取/忽略八度+容差比对/渐进状态机，24 单元测试） |
| 🔁 旋律回声 | ✅ | 入门<b>趣味 + 声光 + 记忆</b>游戏（Simon 记忆机制）：屏幕亮键 + 发声<b>播放一段短旋律</b>，你照着在琴上<b>弹回来</b>——弹对了，旋律就<b>自动加长一个音</b>，越记越长，挑战你的极限！同时锻炼<b>耳朵 / 记忆 / 键盘地理</b>，零基础也能玩。四种<b>音域关卡</b>：🌱五音音阶（C-G 5 音）/🎵五声音阶/🎹一个八度/🌈半音阶（C-C 全 12 音），由易到难。可调<b>速度</b>（60-160），按 ✨弹错时显示<b>正确音名 + 你弹的音</b>对照，记录每关<b>最长记忆长度</b>入仪表盘。和"旋律听写"不同——这里同一条旋律<b>不断生长</b>，是真正的记忆耐力赛。纯逻辑 `melody-echo.js`（序列生成/比对/计分/生长机制，20 单元测试） |
| 🎼 即兴问答 | ✅ | <b>音乐对话 / 即兴启蒙</b>游戏（Call &amp; Response）：钢琴先弹一句<b>问句</b>——它故意<b>不落在主音上</b>，听起来"还没说完、在等你回答"，你在琴上<b>即兴弹一句答句</b>回应。<b>不用照抄！</b>按"音乐性"三件事打分：① 尽量<b>留在音阶里</b>（用高亮的键，主音标"家🏠"金色）② 最后<b>落回主音</b>（给人"说完了"的收束感）③ 有点<b>高低起伏</b>。怎么弹都不算错，越有乐感分越高，实时给出 ⭐⭐⭐ 评星 + 100 分制 + 三项指标条 + 最薄弱项的改进提示。四种<b>调/音阶</b>：🟢C五声音阶（最好听）/🎵C大调/🟡G大调/🔵a小调（忧郁），可调速度。这是练<b>即兴 / 乐句感 / 音阶地理</b>的第一步，特别适合艺术型/不爱机械重复的孩子。纯逻辑 `call-response.js`（音阶/主音、问句生成保证开放、答句音乐性评分，24 单元测试） |
| 🥁 节奏回声 | ✅ | 入门<b>趣味 + 声光 + 节奏记忆</b>游戏（节奏版 Simon）：屏幕<b>亮灯 + 打点</b>播放一段节奏型（长长短短…），你在<b>琴键 / 空格 / 敲击区</b>把它<b>拍回来</b>——拍对了，节奏就<b>自动加长一个音</b>，越拍越长 🎶。这是「旋律回声」的<b>节奏姊妹篇</b>：那个练音高记忆，这个练<b>时值记忆 + 内在拍感</b>，零基础也能玩。判定<b>与速度无关</b>——不要求你卡死在某个 BPM，只看你拍出来的<b>长短比例</b>对不对（小朋友很难精确卡拍，但能记住"长长短短长"的模式）。节奏块<b>宽度随时值变化</b>直观可见，实时给出 ⭐⭐⭐ 评星 + 节奏准度% + 最薄弱段的"太快/太慢"提示。四种<b>难度</b>：🟢长短音（♩𝅗𝅥）/🎵含八分（♪♩）/🟡混合（♪♩𝅗𝅥）/🔴含附点（♪♩♩.𝅗𝅥），记录<b>最长节奏长度</b>入仪表盘。纯逻辑 `rhythm-echo.js`（节奏生成/拍点→间隔/速度归一化比对/状态机，21 单元测试） |
| ↕️ 高低音方向感 | ✅ | <b>零基础音高启蒙</b>第一课：钢琴弹一个<b>参考音</b>（金色键🟡）+ 方向提示（<b>⬆️弹个更高的</b> / <b>⬇️弹个更低的</b>），你在琴上<b>随便弹一个音</b>——只要方向对、又差得<b>够明显</b>就算对，<b>不用找准某个音</b>，怕弹错的孩子也敢探索！练的是最基础的<b>「高音在右、低音在左」「这个音比那个高还是低」</b>概念，是「找中央 C」的姊妹篇（那个练绝对位置，这个练<b>相对高低方向</b>）。答对<b>连击 +1</b>，难度越高要求分辨的高低差越小：🟢大跳（≥八度）/🎵五度/🟡三度/🔴一步之遥（仅 2 个半音）。键盘上<b>参考音金色 + 你弹的音绿/红</b>实时高亮，答错告诉你"方向反了"还是"差得不够"。纯逻辑 `pitch-direction.js`（关卡/出题保证有解/方向幅度判定/状态机，18 单元测试） |
| 🎹 钢琴卷帘播放器 | ✅ | <b>DAW / Synthesia 式</b>整曲可视化：把一首 MIDI 完整"画"成卷帘——<b>左右手不同颜色</b>的音块（右手蓝🔵/左手紫🟣）从左向右流过，<b>橙色播放头</b>扫过时键盘<b>同步亮灯发声</b>、力度映射成音块亮度。内置 2 首示范曲（欢乐颂双手/卡农片段），并<b>支持上传任意 .mid/.midi 文件</b>（复用 `midi-file.js` 解析器，自动识别左右手）。可<b>单独练左手/右手/双手</b>、<b>0.5–1.5× 变速</b>、拖<b>进度条任意定位</b>、开关 🔊声音/🎯播放头跟随滚动。另含 <b>🏓 跟练模式</b>：跟着卷帘在琴上弹，实时统计命中率。定位是"先看整首怎么弹"的预览器，与"曲谱跟弹"判分练习互补。纯逻辑 `midi-player.js`（11 单元测试） |
| 🎼 五线谱播放器 | ✅ | <b>把整首 MIDI 排成真正的五线谱</b>：高音谱号 𝄞 + 低音谱号 𝄢 组成<b>大谱表</b>，音符按音高落到正确的<b>线/间</b>上，<b>自动画加线、升号、符干</b>，右手蓝🔵/左手紫🟣分色。<b>橙色光标</b>横扫时键盘<b>同步亮灯发声</b>、当前音符变金亮，每个音可带音名标签（可关）。内置 2 首示范曲，<b>支持上传任意 .mid/.midi 文件</b>（复用 `midi-file.js` 解析器）。<b>两档模式</b>：🔊 <b>播放</b>（光标横扫、看谱听音）⇄ 🐢 <b>等待练习</b>（Synthesia 学习模式：光标停在<b>当前音符组</b>、该弹的键变金黄，弹齐才前进、<b>不计时不惩罚</b>，按音高<b>忽略八度</b>更友好，卡住按 💡 <b>提示</b>，弹错只闪琥珀不前进（默认开 <b>🌟 容差</b>——弹偏 1–2 个半音也温和放行并提示"差一点点～已帮你过"，对抗 MIDI 小抖动卡死，关掉恢复严格），全部弹完撒花庆祝；<b>屏幕点键和 CA99 真琴双输入</b>都能作答）。可开 🥁 <b>浏览器节拍器</b>（4 拍预备拍 + 自由走拍，<b>不经 CA99、只在浏览器响</b>，帮孩子稳住节奏，节拍可视脉冲）。可<b>练右手/左手/双手</b>、<b>0.5–1.5× 变速</b>、拖<b>进度条定位</b>、开关 🔊声音/🎯跟随/🔤音名。和"钢琴卷帘"互为表里：<b>卷帘看手位、五线谱看读谱</b>，从识谱迈向看谱演奏。纯逻辑 `staff-view.js`（14 单元测试） |
| 🎬 演奏台 | ✅ | <b>「曲谱跟弹」的纯净版独立舞台</b>：只显示<b>三块</b>——上 <b>五线谱</b>、中 <b>落下激光高速路</b>（Synthesia）、下 <b>88 键</b>，去掉一切多余控件，<b>上下空间更舒展</b>、专注演奏。一个大大的<b>「🎵 选择歌曲」按钮</b>弹出<b>触屏友好的选曲面板</b>：📚 内置曲库（🐣 启蒙小曲 + CA99 自带 1543 首，分类 chips + 搜索）/ 🎵 我的 MIDI（扫描 `data/` 目录）/ 📤 上传 .mid，大号可点行、手指轻松切歌。两档：<b>▶ 预听</b>（看音符落、听旋律）+ <b>🎯 跟弹</b>（弹对判 PERFECT/GOOD/MISS、连击计分、★评星）。<b>🎧 先听后弹（#5 铃木教学法，默认开）</b>：跟弹一首<b>还没听过</b>的曲子时，先自动放一遍<b>完整示范</b>（先在脑中建立声音印象），听完<b>自动进入跟弹</b>——同一首听过后再点跟弹则直接开始；可用顶栏开关关掉。<b>关键：🎹 真琴发声默认开</b>——只要 CA99 已连接，预听/示范会<b>直接在钢琴上响</b>（而非仅浏览器音）；未连接才回退浏览器 Web Audio。真琴按键<b>实时点亮屏幕 88 键</b>（霓虹蓝按下特效）。复用 `score-follow.js` 引擎 + `midi-file.js` 解析 + `PianoKeyboard`，**不改动原「曲谱跟弹」模块**。<b>🖐️ 左右手分开练</b>：载入含双手的 MIDI 时自动出现<b>双手/右手/左手</b>切换，选单手时<b>另一只手的音符在高速路与五线谱上淡显</b>、键盘只亮该练的手，方便上下手分别攻克（与曲谱跟弹一致）。<b>🌈 通关灯光秀奖励</b>：完整跟弹完一首会触发庆祝——全局 `victoryLightShow()` 让 <b>CA99 真琴亲自奏一段上行五声音阶华彩 + 主和弦</b>（未连真琴则电脑发声优雅降级）配合满屏撒花，弹得越好（星越多）撒花越隆重；🧩 五度圈拼图集齐全 12 个调时同样触发。把每次「通关」做成有仪式感的奖励时刻（对易放弃的孩子尤其重要）。<b>📖 课本谱面叠加</b>同样可用：选到旁边带同名切图文件夹（index.json + m001.png…）的曲目（选曲面板该行有 <b>📖 谱</b> 角标），五线谱上方会出现<b>真实课本谱面卷帘</b>，跟随演奏高亮当前小节并自动滚动，让孩子<b>只看 app 一块屏</b>即可对照真书演奏。 |
| 🎸 乐队伴奏 | ✅ | <b>Tomplay 式"和乐队一起弹"</b>：载入一首<b>多轨 MIDI</b>，自动把它拆成若干<b>声部</b>（按 MIDI 通道/音轨分组，用 GM program 音色号映射成 🎹钢琴/🎸贝斯/🎻弦乐/🎺铜管/🎷管乐/🥁鼓组等乐器家族），<b>推荐音域最高的旋律声部为"我来弹"</b>。点任意声部卡片可改选"我来弹"——<b>这个声部静音留给 Lily 在 CA99 上真弹</b>，其余声部由电脑用<b>不同音色当"乐队"</b>一起演奏，让她不再一个人孤单练琴、跟着整支乐队合奏（情感价值）。<b>关键：乐队声部只走浏览器 Web Audio 合成（每个乐器家族不同波形/包络，鼓用噪声爆破），绝不发往真琴</b>——真琴完全留给 Lily 弹，互不抢声。<b>🐢 50–120% 变速</b>（慢练）、<b>🔊 乐队音量</b>、<b>🥢 预备拍</b>（开头 4 拍 count-in 对齐）、<b>🔁 循环</b>、<b>🥁 含鼓开关</b>。落下高速路只显示<b>你要弹的声部</b>音符 + 88 键提示该弹的键；跟着弹中时真琴/屏幕点键命中会给 👍🎵⭐ 鼓励（无压力、不扣分）。选曲面板：📚 内置曲库（离线 🎼 小星星合奏示范 + CA99 自带 1543 首）/ 🎵 我的 MIDI（扫描 `data/`）/ 📤 上传 .mid。纯逻辑 `backing-band.js`（11 单元测试）+ 增强的 `midi-file.js`（捕获 program-change）。默认归为 🌱 初级。 |
| 🐉 Boss 战 | ✅ | <b>把一小段难乐句变成"打怪游戏"</b>，专治易放弃的孩子：按顺序在 88 键上弹对乐句就给 Boss <b>削血</b>，弹够 3 遍<b>击败</b>它得奖励！<b>关键是零时间压力</b>（等待模式：按高亮键、弹对才前进），弹错只掉一颗 ❤（有容错、不直接判负），已削的血<b>保留</b>不回。4 个由易到难的 Boss（3~6 音乐句：🟢 史莱姆 / 🦇 蝙蝠 / 🗿 巨人 / 🐉 飞龙）。50 格<b>血条</b>、❤ 生命、乐句进度 chips（当前音高亮）全程可视；击败时 Boss 抖动 + 飘 <b>-伤害数字</b> + 全屏撒花 + ⭐ 评星（<b>无伤=3 星</b>）。可接 CA99 真琴（物理按键直接判定）或点屏幕键盘；<b>忽略八度</b>默认开（任意八度同名键都算对）。纯逻辑 `boss-battle.js`（11 单元测试），成绩入仪表盘。<b>💛 二次机会</b>：心被掉光时，若当天还有<b>复活币</b>（每日免费补 5 颗，`revive.js`）就能<b>满血原地复活</b>、削过的血都保留——专为对抗「怕失败就不敢挑战」而设（降挫败，非表现奖励）。 |
| 🚀 极速挑战 | ✅ | <b>把音阶/琶音弹对、再弹得更快</b>——引擎给整段<b>计时</b>，把第一个音到最后一个音的耗时换算成<b>有效速度 (BPM)</b>；<b>超过当前目标就升一档</b>(+5 BPM) 并刷新你的<b>个人纪录</b>！没达标就再试、弹错从头来（都<b>不惩罚</b>速度）。<b>关键：不强制跟节拍器</b>（"卡点"压力对易放弃的孩子最劝退），只跟<b>自己的纪录</b>比，越刷越快，建立"我在进步"的成长型心态。三块仪表盘（🎯 目标 BPM / 🏅 个人纪录 / ⏱️ 上次速度）+ <b>速度成长曲线</b> sparkline（每次完成画一个点，刷新纪录的点变金）。4 段由易到难的乐句（C 大调音阶 ↑/↓/上下行、C 大三琶音）。88 键<b>等待模式高亮该弹的键</b>（按顺序逐个亮），命中闪绿、错音闪红 + 温和提示。可接 CA99 真琴（物理按键直接计时）或点屏幕键盘；<b>忽略八度</b>默认开。纯逻辑 `speed-run.js`（8 单元测试），成绩入仪表盘。 |
| 🎲 骰子热身 | ✅ | <b>不知道今天先练啥？掷骰子！</b>——专治"选择即压力"的孩子：<b>掷骰子</b>随机生成一张超小的热身任务卡（<b>调性 × 音型 × 手别 × 玩法风味</b>，如「A 大调 · 音阶 ↑↓ · 左手 · 慢慢来」），不用自己挑、照着骰子玩就好。可弹的音型（音阶 ↑/↓/↑↓、大三琶音）会生成<b>音符序列</b>，88 键<b>等待模式高亮该弹的键</b>，孩子按顺序弹对就<b>完成一张</b> → 撒花 + ✅ 计数 + 🔥 连续无错；弹错只是<b>从头再来</b>（温和"再试一次～别急"、不惩罚）。骰子有掷动动画，任务卡四色分面（调/音型/手/风味）。可接 CA99 真琴（物理按键直接判定）或点屏幕键盘；<b>忽略八度</b>默认开。纯逻辑 `dice-warmup.js`（12 单元测试），成绩入仪表盘。 |
| 🎯 练习宾果 | ✅ | <b>把"练习清单"变成宾果游戏</b>——一张 <b>5×5 任务卡</b>，每格一个超小任务（弹音阶/找中央 C/弹首爱的曲子/闭眼弹/即兴 5 个音…），做完<b>点亮</b>它；连成一整<b>行/列/斜线</b> → 撒花奖励 🎉，<b>整张填满</b> → 大奖励 🏆。中心 ⭐ 是<b>免费格</b>（已点亮、白送一格助攻）。<b>任务每天换一批</b>、当天<b>进度自动保存</b>（localStorage 按日期）——不用一口气做完，做一点点亮一点点、积少成多（专治"练同一样东西"的厌烦 + 易放弃孩子的"做不完"焦虑）。可手动「🔄 换一张新卡」。点亮有弹跳动画、连线金色高亮。23 个多样任务池，老师/家长可按当前曲目扩充。纯逻辑 `bingo-card.js`（11 单元测试），成绩入仪表盘。 |
| 🕵️ 猜歌视奏 | ✅ | <b>把视奏变成侦探游戏</b>——专治"读谱枯燥"：屏幕只给<b>五线谱</b>（藏住曲名 🕵️ ？？？），孩子<b>照着谱把开头旋律弹出来</b>（练真视奏，88 键<b>等待模式高亮该弹的键</b>、逐音校验），弹完<b>自动回放一遍</b>（听到自己读出来的旋律 → "啊原来是这首！"），再从 <b>4 个选项</b>里<b>猜是哪首歌</b>，猜对揭晓曲名 + 撒花 🎉。猜错只是"再想想、听听旋律再猜"（<b>不扣分</b>、可重猜、可🔊再听一遍）。全部用<b>公有领域</b>童谣（小星星/玛丽有只小羊羔/欢乐颂/两只老虎/伦敦大桥/划船歌/铃儿响叮当/黑羊咩咩叫），<b>避免版权问题</b>。多音符五线谱实时高亮（当前音黄、已弹绿）。可接 CA99 真琴（物理按键直接判定）或点屏幕键盘；<b>忽略八度</b>默认开。纯逻辑 `guess-song.js`（12 单元测试），成绩入仪表盘。 |
| 🎢 力度过山车 | ✅ | <b>CA99 硬件独家</b>（真实力度感应才好玩）：屏幕画一条<b>起伏的轨道</b>（小山坡/山谷/楼梯/波浪/锯齿 6 形状），孩子用<b>触键的轻重</b>"开车"经过每一站——<b>轨道高 = 弹得响、轨道低 = 弹得轻</b> 🎢。按 note-on 的 velocity 和站点目标力度比，越接近评价越高（🌟perfect ≤8 / 👍good ≤18 / 🆗ok ≤32 / 💨miss），连续接近攒连击、满分给星级。轨道用 SVG 画出折线 + 每站 pp~ff 力度标签 + 🚗 小车 + 已弹站点的<b>粉色实际落点</b>（直观看出弹太响/太轻）。<b>连 CA99 真琴</b>才能感应触键力度（最好玩，走 `coasterOnNote` 钩子）；没真琴时用<b>力度滑杆 + "弹这一站"</b>模拟，方便随时玩/测试。零惩罚向反馈，练强弱控制（音乐表现力）。复用 dynamics-trainer 6 档力度。纯逻辑 `velocity-coaster.js`（19 单元测试）。默认归为 🌱 初级。 |
| 🌲 和弦寻宝 | ✅ | <b>把"认和弦的组成音"变成寻宝游戏</b>（复用 chord-detect 识别引擎）：给一个<b>和弦名</b>（C/Am/G7…），孩子要在琴上<b>把它的每个组成音都弹一遍</b>——每弹对一个，树上点亮一颗果子 🍎；集齐全部组成音 → <b>整棵树发光通关</b> 🌳✨。<b>八度无关</b>（哪个八度的 C 都算）。弹到非组成音<b>不扣分</b>，温和提示"再找找"。3 级难度累进：🌱白键三和弦 / 🌿含黑键三和弦+sus / 🔥七和弦（4 音）。可开<b>"提示位置"</b>在键盘上高亮该按的键（淡蓝=待找、绿=已点亮），熟了关掉自己找。可接 CA99 真琴（`toneTreeOnNote` 钩子）或点屏幕键盘。纯逻辑 `tone-trees.js`（14 单元测试），成绩入仪表盘。默认归为 🌿 进阶。 |
| 🏓 弹球接音 | ✅ | <b>Theta 最上瘾玩法搬到 88 键</b>：一颗颗<b>音球</b>从顶上掉下来，每颗带一个音（球面写音名），趁它落到底之前在琴上<b>弹出对应的音</b>把它"弹回去"接住 🏓——接住攒连击、攒星级。<b>八度无关</b>（任意八度的 C 都接得住）。弹错音<b>不扣分</b>（只断连击，温和），漏接也<b>不会输</b>（无 game-over）。开<b>🔊 听音接</b>球会变空白、落下瞬间发声，靠<b>耳朵</b>找出是哪个音（视唱练耳）。3 级音池：🌱五声 / 🌿C大调 / 🔥半音；球数 8/12/16、3 档速度可调。canvas 分色发光球 + 球拍参考线 + 地面线脉冲 + 漂浮判定字。可接 CA99 真琴（`paddleOnNote` 钩子）或点屏幕键盘。纯逻辑 `paddle-tones.js`（14 单元测试），成绩入仪表盘。默认归为 🌿 进阶。 |
| 🧩 节奏拼图 | ✅ | <b>柯达伊节奏卡拼图</b>（只判节奏不判音高，门槛低）：先点 <b>▶ 听一听</b> 电脑拍出一条节奏，再从下面的<b>节奏卡</b>（ta 一长音 / ti-ti 两短音 / ta-a 二拍 / 休止 / tika-tika / ti-tika）里<b>挑出来拼成一样的</b>，拼满一小节就能<b>检查</b>。<b>按结构指纹比对</b>——等价摆法也算对；<b>拼错不扣分</b>，多听几遍就好。3 难度（🌱简单 ta·ti-ti / 🌿中等 +ta-a·休止 / 🔥挑战 +tika-tika），拍号 2/4·3/4·4/4、速度可调；撤销 / 清空 / 换一题。培养<b>读节奏 + 内在拍感</b>，复用节拍音效。纯逻辑 `rhythm-puzzles.js`（13 单元测试），成绩入仪表盘。默认归为 🌿 进阶。 |
| 🎨 音色猜猜乐 | ✅ | <b>CA99 硬件独家</b>（346 音色是杀手锏，别的 app 物理上做不了）：每题<b>随机切到一个乐器音色</b>（`applySound` 偷偷换真琴音色），孩子<b>随便弹几下听音色</b>，再从 <b>4 个乐器</b>里<b>猜这是什么乐器</b>，猜对 +分 + 撒花 🎉，猜错<b>不扣分</b>只断连对、可重猜。<b>干扰项优先同家族</b>（钢片琴 vs 八音盒 vs 钟琴——越像越难，渐进加难度，满足"同 category 干扰"）。23 种可辨识乐器分 6 大家族（🎹键盘/🔔敲击/⛪管风琴/🎻弦乐/🎸吉他/🎤人声），中文名 + emoji。连对≥3 有加成；最高连对入仪表盘。<b>需连 CA99 真琴 + 开「预听走真琴」</b>才听得到真实音色（已在 UI 提示；没连真琴时电脑只有一种声音）。纯逻辑 `timbre-guess.js`（16 单元测试）。默认归为 🌱 初级。 |
| 🏅 奖牌墙 | ✅ | <b>奖牌只升不降的收集面板</b>——专治固定思维的"重弹焦虑"：每首曲存一枚 <b>🥉铜/🥈银/🥇金/💎白金</b> 奖牌（按正确率 50/70/90/100%&全 PERFECT），奖牌<b>永不下降</b>，只在弹得更好时升级 → 再弹一遍<b>零风险</b>，最差也是维持原奖牌。在 🎼 曲谱跟弹 / 🎹 演奏台 完成一遍即自动评牌，升级时撒花 + 飘字庆祝。面板展示全曲库卡片墙（未获得=🔒 灰色虚线），顶部 💎🥇🥈🥉 计数 + 「已收集 X/Y 枚」彩色进度条，鼓励"收集所有白金"。纯逻辑 `medals.js`（13 单元测试）。默认归为 🌱 初级。 |
| 🌡️ 练习热力图 | ✅ | <b>按「多久没练」着色的技能面板</b>（灵感 ABRSM Scales Trainer）——🟢 刚练过 / 🟡 有点久 / 🔴 该复习啦 / ⚪ 待探索。关键：看的是<b>时间不是对错</b>，红色只表示"它想你啦，回来玩一下"，比"正确率红"对易放弃的孩子温柔得多。每次任意练习结束自动 `touch` 该技能时间戳；卡片<b>越久没练越靠前</b>（温柔提醒去复习），从未练的排最后显示"待探索"。顶部 🟢🟡🔴⚪ 计数 + 「X/Y 技能热乎着」进度条，目标=把整面板点亮成绿。纯逻辑 `heatmap.js`（13 单元测试）。默认归为 🌱 初级。 |
| 🔄 智能复习 | ✅ | <b>间隔重复（spaced repetition）的「今日复习卡」</b>——叠在热力图的 per-skill「上次练习+累计次数」之上，按<b>遗忘曲线</b>算出今天最该回来复习的前 3 项。用 Leitner 盒 / SM-2-lite 的<b>渐长间隔</b>（练 1 次→1 天后复习，2→2 天，3→4 天，4→7 天，5→15 天，≥6→30 天）：<b>练得越熟、下次排得越久</b>=科学地"少练、记得牢"。延续热力图温柔基调——到期不是"你不行"是"这个技能想你啦"💚，到期项按<b>逾期比</b>（daysAgo/interval，越急越前）排，逾期 ≥2 倍标 🔴 醒目；每项一个「去复习 →」直接跳到对应模块。从未练过的归<b>⚪ 待探索</b>邀请（点 chip 去探索，<b>零压力</b>，绝不算"逾期"）；今天没到期项时显示"技能都还新鲜，去探索新玩法吧"。任意练习后 `reviewQueueOnUpdate` 即时重排。纯逻辑 `review-queue.js`（14 单元测试）。<b>直接服务家庭北极星「均衡」</b>——自动把被冷落的技能维度顶上来，防任何维度被长期忽略。默认归为 🌱 初级。 |
| 🌟 成长之星 | ✅ | <b>8 星微进度面板</b>（灵感 Meludia）——把每个 🌱 初级技能拆成 <b>8 颗小星</b>，按「累计练对数」点亮（阈值 1/5/12/25/45/75/120/200），第 1 颗只要练对 1 个就到手 → 即时成就感，单元内<b>胜利时刻 ×8</b>。星星<b>只升不降</b>，每一小步都被庆祝（点亮新星飘字、集满 8 颗撒花）。卡片<b>星少的排前面</b>（最需要鼓励的优先），显示金色实星 + "再练对 N 个点亮下一颗"。顶部「已点亮 X/Y 颗星 · N 个满星」进度条，目标=把所有技能集满 8 颗。纯逻辑 `microstars.js`（12 单元测试）。默认归为 🌱 初级。 |
| 🔥 打卡日历 | ✅ | <b>把连胜引擎「露出来」做成火焰打卡日历</b>（灵感 Duolingo 连胜，防 Break Effect）——专治"断一天就放弃"：6 周日历网格，<b>每天来练就点亮一团 🔥</b>，当日练得越多火越旺（按 session 数分 4 档强度），今天的格子带高亮光环。顶部火焰横幅显示「连续打卡 N 天 🛡️/今天已打卡 ✓」；三枚统计 pip「🔥 当前 / 🏆 最长 / 🗓️ 累计打卡」。关键温柔设计：每周一张 🎟️ <b>豁免券</b>自动兜底（漏 1 天<b>不断连胜</b>，复用引擎 `forgive:1`），回来时只说「<b>欢迎回来 💚</b>」绝不惩罚——只记录"你来过"，不打分、不评判。数据复用现有 `practice-stats.js` 连胜引擎，每次练习经 `streakCalOnUpdate` 实时刷新。纯逻辑 `streak-calendar.js`（14 单元测试）。默认归为 🌱 初级。 |
| 🎁 音色盲盒 | ✅ | <b>练琴的惊喜奖励 × 音色图鉴</b>（可变奖励 + 收集欲，Octalysis 拥有感）——专治"练琴动力不足"：每完成一次练习有 <b>10% 概率开出盲盒</b>，从 CA99 的 346 音色库里随机解锁一个<b>好玩音色</b>（大键琴 / 音乐盒 / 弦乐 / 合唱 / 颤音琴…），攒成一本 <b>40 格音色图鉴</b> 📖。开盒触发全屏盒子弹跳 + 撒花庆祝；图鉴里已解锁卡可<b>点击切到该音色 + 屏幕琴键试听</b>（接上 CA99 就在真琴上响）。可变奖励是最强行为维持机制，<b>不是每次都开</b>反而让人想一直练；内容成本≈0（直接复用现有音色库），且<b>这是 CA99 owner 独有的玩法</b>。音色池按乐器家族跨分类轮选保证多样。纯逻辑 `mystery-box.js`（11 单元测试，可注入 storage+rng），经 `mysteryRoll()` 在 `recordPractice` 后摇盒、`mysteryOnUpdate` 刷新图鉴。默认归为 🌱 初级。 |
| 🎯 今日目标 | ✅ | <b>每天让孩子「自己选」一个今日小目标</b>（SDT 自主感，专治"练琴=被布置的义务"）——开场按日期<b>确定性</b>地推荐 2-3 个玩法（同一天刷新不变、换天换批），孩子<b>自己挑一个</b> → 变成今天的目标，配「去练 →」一键跳到该模块。目标<b>超小化</b>：练 1 次就达成，给一个大大的 ✅ + 撒花 + 「你说到做到 🌟」。达成检测复用 `recordPractice`（练的正是所选模块即触发）。顶部显示「🏅 累计达成 N 个每日目标」建立"我说到做到"的身份。是<b>你的选择</b>而非作业——降低抗拒、迈出第一步最重要。纯逻辑 `daily-goal.js`（12 单元测试，确定性日选 + 跨天重置但保留累计），经 `dailyGoalProgress()` 检测、`dailyGoalOnUpdate` 刷新。默认归为 🌱 初级。 |
| 📊 家长周报 | ✅ | <b>一键把这一周的练习汇总成「本周亮点」家长卡</b>（关联感——给家长一扇了解孩子状态的窗，喂"家长认可"）——聚合已有 `practice-stats`（每日练习次数 / 各模块统计 / 连胜 / 正确率）+ `heatmap`（技能覆盖面 / 待复习数），在屏幕显示<b>四个大数字</b>（本周练习次数 / 到琴前天数 / 连练天数 / 涉猎技能数）+ <b>成长向趋势</b>（比上周多练几次 📈，从不说"退步"）+ <b>最常玩的练习</b> chips + <b>本周亮点</b>列表 + <b>家长寄语</b>，并画成一张 `<canvas>` 卡片可 <b>⬇ 保存 PNG</b> 或 <b>📤 分享给家人</b>（Web Share API，不支持则回退下载）。设计取向<b>陪伴与成长、不是 KPI 考核</b>：少练的一周只读作"下周的新机会 🌈"，措辞全程成长向。任意练习后 `parentWeeklyOnUpdate` 即时刷新。纯逻辑 `parent-weekly.js`（17 单元测试），canvas 画图在 app.js。默认归为 🌱 初级。 |
| 🎬 演奏卡 | ✅ | <b>一键把演奏变成漂亮的「分享卡」</b>（关联感——喂"家长认可"，绕开完整家长端的高成本）——录下弹奏（复用全局 `recorder` 录 MIDI 事件），点「✨ 生成分享卡」即在 `<canvas>` 上画出一张卡：<b>渐变背景 + 曲名 + 日期 + ⭐ 评星 + 你弹的音符迷你卷帘缩略图（按音高 HSL 上色）+ 音符数/时长/音域 + 一句鼓励语</b>，可<b>⬇ 保存 PNG</b> 或 <b>📤 分享</b>（Web Share API 带文件，不支持则回退下载）发给爸爸妈妈。评星<b>鼓励向</b>：只要弹了就 ≥3 星，越多/越久越高（封顶 5），永远不惩罚。纯逻辑 `share-card.js`（20 单元测试：extractNotes 还原音符 / summarize / starRating / praiseLine / rollLayout / fmtDuration / midiName），canvas 画图在 app.js。默认归为 🌱 初级。 |
| 🚀 看谱击落 | ✅ | <b>Staff Wars 式街机认谱游戏</b>（把枯燥的认谱卡变成太空射击）——音符像「入侵者」从右边飞向左侧炮台，<b>读出音符 → 在真琴/屏幕键盘弹出来</b>就发激光击落它 💥；飞到炮台会少一颗 💛（共 3 条命），得分到阈值<b>自动升级提速</b>，挑战个人最佳（localStorage）。按<b>音名</b>判定——哪个八度都算对（初学者友好），可选「显示音名提示」在符头标字母作脚手架。canvas 渲染星空 + 五线谱（高/低音谱号，自动加线）+ 符头符干 + 激光/爆炸特效；弹错<b>不扣分</b>只轻微震动（低挫败）。真琴经 `staffWarsOnNote` 钩子驱动、屏幕键盘点击亦可。纯逻辑 `staff-wars.js`（18 单元测试：种子 rng 确定性 / 生成 / tick 移动与越界丢命 / 命中最危险者 / 升级提速 / 音名匹配 vs 精确 / `revive()` 复活）。<b>💛 二次机会</b>：命被打光时，若当天还有<b>复活币</b>（每日免费补 5 颗，`revive.js`）就能<b>满血原地复活</b>、得分和关卡都保留——对抗挑战回避（降挫败，非表现奖励）。默认归为 🌱 初级。 |
| 🫧 接音水滴 | ✅ | <b>Theta「Drops」式听辨 / 键盘地理游戏</b>（比看谱击落更轻松、无输赢）——彩色音符水滴从天上落下 🌧️，<b>在真琴/屏幕键盘弹出对应的音</b>就把它接住 🫧，接住爆出光环；漏到地面只是<b>断连击不结束</b>（低挫败），重在多接、刷新「最佳连击」（localStorage）。两种模式：<b>看名模式</b>水滴上显示音名（认键）、<b>听音模式</b>水滴一出生先放出声音让孩子<b>用耳朵找键</b>（听辨）。三档下落速度 🐢🚶🏃。按<b>音名</b>判定（哪个八度都算对，初学者友好，可选精确）。canvas 渲染按音名分色（C 红…B 紫）的发光水滴 + 地面水池 + 接住光环动画；连击每 5 个弹鼓励，最佳连击撒花。真琴经 `dropsOnNote` 钩子驱动、屏幕键盘点击亦可。纯逻辑 `drops.js`（12 单元测试：种子 rng 确定性 / 生成 / tick 下落与落地断连击 / 接住最危险者 / 连击与最佳连击跨断点 / 音名匹配 vs 精确）。默认归为 🌱 初级。 |
| 🧩 五度圈拼图 | ✅ | <b>把五度圈做成闯关解锁拼图</b>（建立调号肌肉记忆 + 收集成就感）——从 C 大调出发，沿五度圈顺时针 <b>C → G → D → A …</b>，<b>弹对当前这一格的整条大调音阶</b>就点亮它 🔓、推进到下一格，把 12 个调一格格收集齐；进度持久化（localStorage），可「重新开始」。canvas 渲染 12 格圆环（已解锁=分色发光+显示关系小调、当前目标=金色脉冲 🎯、未解锁=灰色 🔒），右侧显示目标调名 + 调号（几个升/降号）+ 音阶音名条（弹对的音变绿 ✓、下一个高亮脉冲），弹错只温和提示「下一个该弹 X」<b>不惩罚</b>。按<b>音高类</b>判定（哪个八度都算对）。真琴经 `cofPuzzleOnNote` 钩子驱动、屏幕键盘点击亦可。纯逻辑 `cof-puzzle.js`（复用 `circle-of-fifths.scaleMidi`，12 单元测试：初始仅 C 解锁 / 弹全 G 音阶解锁并推进 D / 弹错不前进 / 音高类 vs 精确 / 存读档 / 全清重置 / 12 格全解锁）。默认归为 🌱 初级。 |
| 🏆 成就仪表盘 | ✅ | 汇总视奏/听辨/力度/音阶/节奏/旋律/和弦进行/节拍稳定度/双手协调/琶音跑动/连奏断奏/踏板时机/颤音速度/装饰音/音程大跳/旋律突出/力度渐变/速度渐变/复节奏/颗粒性/手指独立性/音阶八度跨度/节奏听写/移调视奏/和弦转位听辨/调号识别/音阶指法提示/音程构建/调式识别/唱名听辨/和弦性质听辨/和声进行听辨/曲谱跟弹/终止式辨认/键盘音名认知/五线谱识谱卡/乐句视奏/和弦视奏/节奏视奏/伴奏音型/和弦色彩板/旋律回声/即兴问答/节奏回声/高低音方向感各训练成绩，统计总练习次数/正确率/连续天数/最佳连击，最近 7 天柱状图、模块细分表、10 枚成就徽章墙（已解锁/即将解锁/锁定） || 📡 MIDI 监视器 | ✅ | 实时显示钢琴发来的音符/CC/SysEx |

> 后续玩法（自动伴奏/灯光同步）作为新模块加入 `app.js` + 侧栏，不另起 app。

### 已知协议坑点

#### 🥁 节奏（鼓点）模块

CA99 的节拍器内部有两个子模式，**必须按顺序发三条 SysEx**，单独发 `RhythmSelect` 无效：

| 步骤 | SysEx | 说明 |
|---|---|---|
| 1 | `MetronomeMode = Rhythm` (`fn=0x10 v1=0x56 v2=0x0A v4=0x01`) | 切到鼓点模式（默认是普通节拍） |
| 2 | `RhythmSelect = [index]` (`fn=0x10 v1=0x56 v2=0x09 v4=[0-99]`) | 选节奏型 |
| 3 | `MetronomeRun = Start` (`fn=0x10 v1=0x56 v2=0x08 v4=0x01`) | 启动 |

切换节奏时需要先 **Stop → Mode → Select → Start**，热切（播放中直接换）无效。
`ca99.js` 已提供 `buildMetronomeMode(mode)` 和 `buildMetronomeRun(running)` 函数。

### 模块实现说明

- 每个模块是 `app.js` 里一个 `renderXxx()` 函数，渲染到对应 `#module-xxx` 容器
- 共享 `midi-core.js`（连接）+ `ca99.js`（协议）+ 数据表
- 🎹 <b>键盘配色统一（与「曲谱跟弹」Synthesia 对齐，且随主题切换）</b>：键盘高亮色不再写死常量，而是从 CSS 变量读取——`--kb-hand-l`（左手紫）/`--kb-hand-r`（右手青）/`--kb-cue`（cue 提示该弹哪键，琥珀）。app.js 用 `refreshThemeColors()` 把这三个 CSS 变量缓存进 `KB_COL`（避免热路径反复 `getComputedStyle`），helper `kbHandColor(hand)`/`kbCueColor()` 读缓存；启动与<b>切换主题</b>时刷新缓存，键盘 flash 色自动跟随当前主题。曲谱跟弹/演奏台/MIDI 钢琴卷帘播放器/五线谱播放器五处键盘高亮全部走这套 helper，新增带键盘的模块复用即自动对齐，<b>勿再写内联手别色</b>（历史上演奏台 cue 用过灰/琥珀、卷帘与五线谱用过 `#d59bff`/`#60a5fa`，已统一）
- 🎨 <b>落音符方块 / 五线谱音符头配色统一（同样对齐曲谱跟弹）</b>：在 `app.css` 的 `:root` 定义单一数据源 CSS 变量——`--note-r:#667eea`（右手 indigo）/`--note-l:#a78bfa`（左手 purple）+ 对应描边 `--note-r-stroke`/`--note-l-stroke`，落音符方块另有 `--note-block-r`/`--note-block-l`（带渐变）。覆盖：落音符方块 `.scf-note`（曲谱跟弹/演奏台）、`.mpl-n-r/.mpl-n-l`（MIDI 钢琴卷帘，原 `#3b82f6`/`#a855f7`）、`.sv-n-r/.sv-n-l`（五线谱播放器，原 `#60a5fa`/`#c084fc`）；五线谱音符头 `.scf-staff-svg .note-head`(.nh-left)、`.sight-svg .note-head`、`.sp-head`、`.rs-head` 全部走 `var(--note-r/-l)`。判定态色（`.n-due` 琥珀/`.n-perfect` 绿/`.n-good` 青/`.n-miss` 粉、`.nh-*`/`.rs-*`/`.sp-*` 同款）本已一致，未动。<b>勿再写内联音符填充色</b>，新增带落音符/五线谱的模块复用这套变量即自动对齐
- 🌈 <b>全局界面主题（一键切换全 app 配色）</b>：顶栏 `#theme-select` 提供 5 套主题——🌙 午夜（默认 indigo/紫）/🌊 海洋（青蓝）/🍭 糖果（粉紫）/🌲 森林（绿）/🔥 暮光（橙红）。切主题＝改 `<html data-theme>` 一个属性，全 app 的<b>背景/面板/强调渐变/键盘 flash 手别色/落音符方块/五线谱音符头色</b>一次性跟随（都已 token 化）。`app.css` 里 `:root` 为默认主题、各 `[data-theme="X"]` 块覆盖同名「外观/品牌」token；<b>语义反馈色（`--hi` 红=错 / `--ok` 绿=对 / `--warn` 黄=提示）刻意不进主题覆盖范围——跨所有主题保持不变，确保「红=错/绿=对」的含义在任何主题下都一致，孩子不会混淆</b>。5 套均为深色家族（与众多假设深背景的模块硬编码色安全共存，浅色主题因风险过高已弃）。纯逻辑 `theme.js`（`THEMES`/`getTheme`/`setTheme`/`applyTheme`/`initTheme`，16 单元测试）；选择持久化到 localStorage `ca99_theme`，启动即应用（`initThemeUi()` 在 `main()` 最前、render-all 之前）；切主题派发 `ca99:theme-change` 事件并 `refreshThemeColors()` 刷新键盘缓存色
- 自动换音色用独立引擎 `auto-rotate.js`（纯逻辑，21 单元测试），UI 在 app.js
- **节拍模式**：监听 MIDI 输入的 note-on 驱动 `engine.tick()`，弹够 N 个音符就换
- VT 渐变用独立引擎 `vt-morph.js`（纯逻辑，32 单元测试），定时器逐帧线性/缓动插值
- **可渐变参数识别**：取 `sysex.json` 中 v1=0x50 且恰好 2 条值（min/max 范围）的连续型参数（如 StringResonance 0-127、DamperResonance 0-10），排除枚举型（Voicing）和命令型（PerNote）
- 力度换音色用独立路由 `velocity-switch.js`（纯逻辑，33 单元测试），`splitZones()` 把 0-127 等分成层，note-on 力度命中哪层就切到该音色（仅在层变化时发 PC，避免每音符重复）
- 力度→VT 联动用独立引擎 `vel-vt-link.js`（纯逻辑，22 单元测试），`mapRange()` 把力度线性映射到 VT 输出范围，指数平滑（EMA）防抖，仅整数值变化时发 SysEx。可多通道、可反向映射
- VT 连续参数检测抽成共享 `continuousVtParams()`，渐变器与联动模块共用
- 踏板控制用独立引擎 `pedal-control.js`（纯逻辑，27 单元测试），解析标准踏板 CC（延音64/保持66/弱音67/表情11），实时汇报开关+深度，并可把某踏板深度 `mapRange` 映射到一个 VT 参数（仅整数值变化时发 SysEx，支持反向）
- 演出预设用独立存储 `preset-store.js`（纯逻辑，40 单元测试），存储后端可注入（浏览器=localStorage，测试=内存 Map）；支持保存/调用/删除/重命名/导入导出，损坏 JSON 容错。`capturePreset()` 从 DOM 抓当前音色+VT 控件值，`applyPreset()` 一键发回钢琴
- 和弦识别用 `chord-detect.js`（纯逻辑，46 单元测试），把同时按下音符归一到音级集合，对每个根音匹配 15 种和弦模板（含 7/maj7/m7/dim/aug/sus 等），两遍扫描优先根位、消除 sus2/sus4 与 C6/Am7 转位二义性
- 和弦练习挑战用 `chord-trainer.js`（纯逻辑，32 单元测试），`HeldNotes` 追踪当前按下音符，`ChordChallenge` 随机出题+计分/连击（rng 可注入便于测试）。onMidiIn 的 note-on/off 维护 heldNotes 并驱动面板
- 节拍器用 `metronome.js`（纯逻辑，33 单元测试）：`Metronome` 按 BPM 产生强/弱拍（定时器可注入），UI 用 Web Audio 发 click 声 + 节拍点闪烁；`TempoTracker` 用相邻 note-on 间隔的移动平均实测演奏 BPM（大间隔自动重置乐句）
- 录制回放用 `recorder.js`（纯逻辑，38 单元测试）：`Recorder` 以毫秒时间戳记录 MIDI 事件，回放用注入定时器按相对时间重派发，`toMidiFile()` 编码标准 MIDI 文件（Type 0，VLQ delta、tempo meta、跳过非通道事件）。onMidiIn 录制所有输入；导出用 Blob 触发 .mid 下载
- 音阶练习用 `scale-trainer.js`（纯逻辑，41 单元测试）：`buildScale`/`buildScaleUpDown` 生成 8 种音阶（大调/三种小调/五声/布鲁斯/半音阶）的 MIDI 序列，`ScaleSession` 按依次弹奏检查进度（可忽略八度），note-on 驱动前进/报错/完成，UI 高亮当前应弹的音
- 五度圈用 `circle-of-fifths.js`（纯逻辑，21 单元测试）：`WHEEL` 12 格（顺时针每格升五度、关系大小调主音差小三度，6 点钟含 F#/Gb 等音）；`majorScaleSpelling(key)` 用字母序列+半音差算出<b>正确拼写</b>的 7 音音阶（保证 7 个字母不重复，如 G→F#、Db→全降、含必要重升降）；`diatonicChords(key)` 给出顺阶七级三和弦（罗马数字+质量 maj/min/dim+和弦名）；`chordMidi(key,degree,baseC)`/`scaleMidi(key,baseC)` 算 MIDI 供试听；`neighbors(key)` 取顺/逆时针相邻调与关系小调；`signatureLabel` 生成调号文字。UI 用 SVG 环形扇区手绘圆盘（`pt(r,deg)`+`sector(rIn,rOut,a0,a1)` 算annular sector path，0°在 12 点钟顺时针），外环大调/内环小调可点击选调、中心 hub 显示当前调+调号；右侧信息卡列调号/关系小调/音阶/相邻调+7 个配色和弦按钮；点和弦/「播放音阶」/「I–IV–V–I 终止式」用 Web Audio 发声并在 88 键上高亮（主音金色）。纯探索工具不计分
- 视奏闪卡用 `sight-reading.js`（纯逻辑，62 单元测试）：`staffPosition`/`needsLedger`/`randomNote` 把 MIDI 音符映射到五线谱位置（高/低音谱号、自动加线），`SightReadingGame` 随机出题并校验（可忽略八度），记录得分/连击/最佳/正确率，UI 用 SVG 实时绘制谱表+符头，note-on 驱动判分与翻题
- 曲谱跟弹用 `score-follow.js`（纯逻辑，44 单元测试）+ `midi-file.js`（SMF 解析，13 单元测试）：曲谱有两种记谱——内置 7 首公有领域旋律用顺序记谱 `[midi,durBeats]`（`null`=休止，可带 `meter` 拍号供节拍器重拍），上传的 MIDI 经 `parseMidi()` 解析为<b>绝对时间记谱</b> `notes:[{midi,ms,durMs,beat,dur,hand}]`（支持和弦/双手/变速），`songFromMidi()` 转成 ScoreFollow 曲目。`ScoreFollow._build` 兼容两种记谱并用 `timeScale`（默认 1，速度倍率走 `1/speed`）统一缩放时间轴；播放头时间 `t`（毫秒）驱动 `judge(midi,t)`（找最近、音高匹配、在 good 窗内的未判音符，按 `|t−目标ms|` 分 PERFECT≤130ms/GOOD≤320ms，连对累计 Combo+加分）、`expire(t)`（过窗未弹自动判 MISS 并断连击）、`active(t)`/`upcoming(t,ahead)`（取该弹/即将落下的音符），`groups(tol)`（同起音时刻音符归一组，供等待模式逐组推进+和弦显示）、`beatAt(t)`（按 (ms,beat) 线性插值算五线谱光标拍位，兼容变速 MIDI，也用于节拍器整拍触发）、`accuracy`/`stars`（90/70/50% → ★★★/★★/★）。`handFilter`（'both'|'r'|'l'）让 judge/expire/active/total/range 只算该手，实现<b>分手练习</b>。UI 用 `requestAnimationFrame`：上方 SVG 五线谱整曲横向铺开 + 光标随拍前进并自动滚动居中、音符按手别染色（右手蓝/左手紫）/按判定结果染色、非练习手淡显；中间 Synthesia 式下落高速路（用 `piano-keyboard.js` 的 `buildLayout` 取每键 x 中心，音符块按 `(目标ms−t)` 下落到判定线、对齐下方 88 键，<b>块上叠音名标签</b>可关）；落到判定线时高亮该弹的键、弹对/漏弹弹出 PERFECT/GOOD/MISS×Combo 飘字。<b>三档训练</b>：🔊 听示范（自动播放不判分）/ 🐢 等待练习（播放头停在当前组、`waitOnNote` 验证弹齐整组才 `frozen=false` 推进、不计时、💡 提示让该组键闪 3 秒并发声）/ 🎯 跟弹判分（judge 计分）。辅助：🥁 节拍器（`tickMetro` 按 `beatAt(t)` 整拍触发 `clickSound`、`meter` 重拍加重音、`scf-bpm` 徽章显示 `bpm×speed`）、🐇 渐进提速（finish 时正确率 ≥80% 把 `speed` +0.05 上限 1.5× 并 `prepare()` 重建）、🔁 区间循环（按 `meter` 把小节换算成拍区间 `[loopStartBeat,loopEndBeat)`→毫秒，start 时把窗外音符标 `judged+null`（不画/不判/不漏）、wait 模式按窗口过滤 `groups`；frame 到段尾 `resetWindow()` 重置窗内 judged 并把 `t0`/`waitClock` 跳回段首，循环不自动 finish，停止时补记成绩；五线谱画半透明 `scf-loop-region` 标出循环区）。完成后 `timings()` 按起音顺序导出每个音的判定结果与误差（负=抢拍/正=拖拍），`drawTimingChart()` 用内联 SVG 画 ⏱️ <b>落点时间对比图</b>：准点线居中、±perfectMs 绿带/±goodMs 青带，命中点按 `deltaMs/goodMs` 映射到上（抢）下（拖）、MISS 画灰叉，附 `early/late/onTime/avgAbs/maxAbs` 统计与偏抢/偏拖/均衡总评。上传 MIDI 经 `<input type=file>` → `FileReader.readAsArrayBuffer` → `parseMidi` → `songFromMidi` 加入选曲。<b>📚 CA99 自带曲库</b>由 `scripts/build_midi_catalog.py` 读 `reference/.../music.json`（APK 提取的曲名/作曲家/function/category 元数据）把 `reference/midi/` 的 1543 首按 function 分类复制进 `app/midi/<类别>/` 并写 `app/midi/catalog.json`；`renderScoreFollow` 里 `setupCa99Library()` fetch 该 catalog 渲染分类 chips + 搜索 + 分子类列表，点击即 `fetch(path)` → 复用 `loadMidiBuffer()`（同上传一条路径）载入，单首解析失败不影响整体。<b>🎵 我的曲库</b>由 `userlib.js`（纯逻辑，11 单元测试）+ `setupUserLibrary()`/`scanUserDir(base)` 实现：先取 `<base>/userlib.json` 清单（由 `scripts/build_user_catalog.py` 扫 `app/<base>/` 子目录生成，`catalogFromManifest` 解析，兼容静态主机），无清单则 fetch `<base>/` 目录自动索引页用 `parseDirListing` 抽出子目录与 .mid（`python -m http.server` 即可，丢文件刷新即见），`buildUserCatalog` 把「子目录即分类、根级 .mid 归未分类」组装成与 CA99 库同形的 `{total,categories,songs}` 目录；两库共用 `buildLibBrowser({els,catalog,prefix,idleMsg})` 渲染分类 chips+搜索+列表，fetch 时按 `path.split('/').map(encodeURIComponent).join('/')` 逐段编码（曲名/分类带空格也安全），点击复用同一条 `loadMidiBuffer()` 载入。键盘回显：`onMidiIn` 的 note-on/off 经全局 `scfKbEcho`/`scfKbEchoOff` 钩子在<b>任何模式</b>都把真琴按键映射到屏幕 88 键。<b>等待模式容差通过</b>：引擎新增 `waitMatch(midi,groupNotes,{tolerant,semis=2})`（先找精确/八度等价匹配，没有再在 `semis` 半音内取最近的未判音符）+ `accept(note,grade)`（不做音高匹配直接把某音记为已判、累计 good/combo）。scf 的 `waitOnNote` 用它：精确命中走 `judge` 染绿；否则若 `🌟 容差通过` 开（默认）且在 2 半音内 → `accept` 染黄 + 把正确目标闪绿 + 提示"差一点点～已经帮你过啦"；超出 2 半音或关掉容差则严格不前进。staffview 的 `svWaitAnswer` 同理但用音级集合的<b>环形半音距离</b>（`d=|need−pc|; if(d>6)d=12−d; d≤2` 即放行），契合它"忽略八度"的判定。专治 MIDI 小抖动/弹偏 1–2 音让等待模式卡死、孩子受挫。结束写入成就仪表盘
- 📖 课本谱面叠加用 `sheet-index.js`（纯逻辑，23 单元测试）：约定<b>一首 .mid 旁可放一个与文件名同名的文件夹</b>（去掉 `.mid` 后缀），内含 `index.json` + 每小节照片切片 `m001.png m002.png…`（零填充命名、字典序=演奏序，由用户自己的 OMR 切图流程把整页课本照片按小节切出，<b>各切片高度只需一致即可、取任意值</b>——`index.json.height` 只作缩放基准，app 显示高度与之无关）。`index.json` 契约：`{stem, height, bpm, bar_seconds, n_measures, total_seconds, measures:[{i, file, w, t_start, t_end, beat_start, beat_end, low_confidence}]}`（`beat_start/beat_end` 为可选的真实 MIDI 拍区间，见下）。构建脚本 `build_midi_catalog.py` 在生成 catalog 时若检测到 `<类别>/<stem>/index.json` 存在就给该曲打 `sheet:true` 标记（`song-tree.js` 按引用透传，无需改），选曲器据此渲染 <b>📖 谱</b> 角标，并决定是否把 `s.path` 作为 sheetPath 传入（非课本曲绝不发起 fetch）。引擎：`parseSheetIndex(json)` 规范化各小节并<b>累加出每格的源像素区间 x0/x1 与 totalWidth</b>（卷帘是把所有小节切片首尾相接铺成一长条），并捕获每格 `beatStart/beatEnd` + 计算 `hasBeats` 标志（仅当<b>每一格都有合法 `beatEnd>beatStart` 且 `beatStart` 单调非减</b>时为 true，否则回退）。<b>光标定位（核心）</b>：`drawSheet/psDrawSheet` 每帧用 `beat = sf.beatAt(t)`（引擎拍位，随 `timeScale` 慢练/渐进提速自动跟随），再——`hasBeats` 时走 `measureAtBeatRange(measures, beat)`<b>按真实 MIDI 拍区间二分查找</b>（找最后一个 `beatStart<=beat` 的格，算格内进度 `f`）；否则走 `measureAtBeat(beat, totalBeats, nMeasures)` 把整曲<b>均匀</b>分到 n 格作回退。<b>为何要二分而非均匀</b>：真实课本有<b>弱起小节、变拍号、D.S. al Fine + 第二遍高八度反复</b>——有反复时 MIDI 会把某些小节演奏两遍/跳转，线性均匀映射必在反复点崩。OMR 在 `index.json` 给每格标真实拍区间、并把反复段<b>展开成重复条目</b>（同一 `file` PNG、不同 beat 区间），卷帘据此顺演奏序左→右平铺（反复段在条上出现两次），光标只向前推进、无回跳，弱起/变拍/反复全稳；`measureAtBeatRange` 还处理格间间隙（停在前一格末尾 f=1）与两端 clamp。秒数 `t_start/t_end` 会随变速漂移，仅留作 `measureAtTime` 兜底。`cursorX(measures,idx,f)` 算光标在源像素中的位置。UI（scf 的 `loadSheetFor/buildSheetRibbon/drawSheet/scrollSheetTo`、演奏台对应 `psLoadSheet/psBuildSheet/psDrawSheet/psScrollSheet`）：选曲后从 `getCurrentSong()._sheetPath` 派生路径 fetch index.json + 各 png，在五线谱上方插一条 `.scf-sheet-ribbon`（`overflow-x:auto`）内含 `.scf-sheet-inner`（宽=totalWidth×scale，scale=显示高 ÷ 切图源高，<b>显示高与 index.json 的 height 解耦</b>——切图源高只是缩放基准，可任意；显示高 `sheetActiveH()` 优先读 localStorage 手动值，否则 `defaultSheetHeight(window.innerHeight)` 按视口高×0.24 自适应并夹到 [72,360]，<b>平板横屏视口矮→谱面收窄给键盘/光柱留空间、竖屏视口高→谱面放大更易读</b>；面板头部有<b>高度滑杆</b>（72–360，拖动即 `setSheetH` 存 localStorage 并重排，宽高同比缩放保持纵横比）+ <b>⤢ 自适应</b>按钮（清掉手动值回到跟随视口），偏好<b>两 tab 共享并跨刷新持久</b>；监听 `resize`/横竖屏切换，未手动设过高度时自动重排）平铺 `<img>` 切片 + 一条<b>内容坐标系</b>里绝对定位的光标线（放在 inner 内才能随图片一起滚动）；播放/跟弹的每一帧移动光标 + 设 `scrollLeft` 居中 + 给当前格加 `.cur` 高亮 + 更新「第 X / N 小节」标签（`low_confidence` 的格用淡黄 `.low-conf` 标注「识别可能不准」），并提供 ◀▶ 手动翻看。起步曲/上传曲无 `_sheetPath` 故自动清空面板，整个特性对非课本曲零副作用
- 音程听辨用 `ear-training.js`（纯逻辑，49 单元测试）：`INTERVALS` 表（0..12 半音）+ `EarTrainingGame` 随机出根音+音程（方向 up/down/harmonic/mixed，可选音程集合，自动保证音符落在合法 MIDI 范围），`check` 校验并记录得分/连击/最佳/正确率，UI 用 Web Audio 三角波发声播放，点按钮作答（无需连钢琴）
- 力度练习用 `dynamics-trainer.js`（纯逻辑，52 单元测试）：`DYNAMICS` 把 velocity 1..127 无缝划成 6 档（pp/p/mp/mf/f/ff），`velocityToIndex` 定位档位，`DynamicsGame` 出目标力度并按 note-on velocity 校验（容差可调，相邻档可算对），记录得分/连击/最佳/正确率，UI 用刻度色带+指针实时显示你弹的力度落点
- 移调器用 `transposer.js`（纯逻辑，74 单元测试）：`encodeTransposeByte` 把半音 -12..12 编码成 CA99 TransposeValue 字节（0x40+半音，实测 -12→0x34/+12→0x4C），`Transposer` 跟踪移调量、`targetKeyName` 算听感调、`transposeNote` 软件移调音符；UI 用滑块/±按钮/预设切换，`CA99.buildTranspose` 发 SysEx 让钢琴自身发声也移调
- 成就仪表盘用 `practice-stats.js`（纯逻辑，51 单元测试）：`PracticeStats` 注入存储+时钟（便于确定性测试），`record` 把每次练习成绩累加进 localStorage，算总练习/正确率/连续天数（`dayStreak` 含昨日宽限）/最佳连击，`allAchievements` 判定 10 枚徽章解锁状态；各训练模块（视奏/听辨/力度停止时、音阶完成时）调 `recordPractice()` 写入，仪表盘聚合展示柱状图/细分表/徽章墙
- 🏅 奖牌墙用 `medals.js`（纯逻辑，13 单元测试）：灵感来自 Virtual Piano / Melodics 的「permanent personal best」——`Medals` 注入存储+时钟，`MEDAL_TIERS` 定义五档（none/bronze≥50%/silver≥70%/gold≥90%/platinum=100%&全 PERFECT），`medalForResult(accuracy, allPerfect)` 纯函数算应得档位；`award(songId,{accuracy,allPerfect,title})` 取 `max(prev,new)` 档位<b>只升不降</b>并记 `bestPct`/`plays`，返回 `{medal,prevMedal,upgraded,isNew}`，`counts()`/`all()` 供收集面板。app 级单例 `medalStore` + `awardMedal(songId,title,summary)` 包装：从 summary 算 `allPerfect=miss===0&&good===0&&perfect>0`，升级时 `cheerToast`+`cheerBurst` 庆祝并触发 `medalWallOnUpdate`。三个完整曲子完成点（曲谱跟弹 finish/区间循环结束、演奏台跟弹结束）紧挨 `recordBest` 调 `awardMedal`。`renderMedalWall()` 列出 `SCF_SONGS` + 任何已获奖的额外 id（自定义/演奏台曲），每张卡显示奖牌图标（未获得=🔒 灰色虚线）+ 最佳%/弹过遍数，顶部 💎🥇🥈🥉 计数 pip + 「已收集 X/Y 枚」彩色渐变进度条；核心心理设计=<b>重弹零风险</b>对抗固定思维。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 🌡️ 练习热力图用 `heatmap.js`（纯逻辑，13 单元测试）：灵感 ABRSM Scales Trainer——`Heatmap` 注入存储+时钟，按「距上次练习的天数」分档（`HEAT_BUCKETS`：fresh<2d/recent<4d/fading<8d/stale≥8d + `NEVER_BUCKET` 从未练）而<b>非</b>正确率，`bucketForDays`/`daysAgo` 纯函数。唯一漏斗 `recordPractice()` 里调一次 `heatmap.touch(moduleId,label)` 即自动覆盖全部 ~50 个练习模块（标记该技能时间戳+次数）。`all(catalog)` 合并 14 项 `HEATMAP_CATALOG` 核心 level-1 技能（让从未练的也显示"待探索"卡），排序为<b>越久没练越靠前</b>（温柔提醒复习），从未练的排最后；`counts()` 各档计数。`renderHeatmap()` 渲染彩边框卡（左边框=档位色，fresh/recent 带呼吸发光动画），顶部 🟢🟡🔴⚪ 计数 pip + 「X/Y 技能热乎着」进度条 + `heatmapOnUpdate` 实时刷新。设计要点=<b>红/灰表达时间而非失败</b>（"它想你啦"），降挫败。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 🔄 智能复习队列用 `review-queue.js`（纯逻辑，14 单元测试）：<b>间隔重复</b>引擎，<b>不存自己的数据</b>，直接<b>复用 `heatmap.all(HEATMAP_CATALOG)`</b> 的 per-skill `{count, daysAgo}` 作输入——零额外存储成本。`REVIEW_INTERVALS=[1,2,4,7,15,30]`（天），`intervalForCount(count)` 用 Leitner 盒选间隔（练 1 次→第 0 盒 1 天，封顶第 5 盒 30 天，count≤0 返 null=从未练）；`reviewStatusOf(entry)` 算 `{interval, never, due(daysAgo>=interval), overdue(max0,daysAgo-interval), ratio(daysAgo/interval)}`；`buildReviewQueue(entries,{limit=3})` 分三桶——`due`（到期，按 <b>ratio 降序 → overdue 降序 → 名称</b> 排，最急在前）/`explore`（从未练，按名称）/`fresh`（已练未到期，按越接近到期越前）+ `pick=due.slice(0,limit)` 今日卡 + `counts`。设计哲学延续热力图：<b>到期=「它想你啦」非「你不行」</b>，练得越熟间隔越长（科学地少练记得牢），从未练的归"待探索"邀请<b>绝不算逾期</b>不施压。`renderReviewQueue()`（`#module-review`）：今日卡每项含图标/「N 天没练·每 M 天复习一次」/「去复习 →」按钮（经 `GOAL_NAV_MAP[id]||id` 跳到对应模块，复用每日目标的同款跳转），逾期 ratio≥2 标 `.rq-hot` 红醒目；待探索渲染 chip（点击去探索）；无到期项时显示"技能都还新鲜"。任意练习后 `recordPractice()` 紧跟 `heatmap.touch` 调 `reviewQueueOnUpdate()` 即时重排（该技能复习计时重置→移出 due）。<b>直接服务家庭北极星「均衡」</b>：自动把被冷落维度顶到队首，防任一维度被长期忽略；与热力图共用 `HEATMAP_CATALOG`，无新数据/无新副作用。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 📊 家长周报用 `parent-weekly.js`（纯逻辑，17 单元测试）：<b>聚合现有引擎、不存自己的数据</b>——`buildParentReport({days14, moduleStats, snapshot, heatCounts})` 接收已从 `practiceStats.recentDays(14)`/`moduleStats()`/`snapshot()` + `heatmap.counts(HEATMAP_CATALOG)` 抽出的原始数字（纯函数、无 DOM/存储，便于确定性测试）。`splitWeeks(days14)` 把最近 14 天切成「上周 7 天 / 本周 7 天」并各自 `summariseWeek`（sessions/activeDays/perDay/busiest）；`trendOf(thisSessions,lastSessions)` 给<b>成长向</b>趋势（多练=📈"稳步上升"，持平="保持住"，<b>少练绝不说"退步"</b>=🌈"下周再出发"，首周="好的开始"）；`topModules(moduleStats,3)` 取练得最多的前 3 项；`coverageOf(heatCounts)` 派生 `{practiced, total, needReview(fading+stale), never}`；`highlightLines(ctx)` 拼 2-4 行家长视角亮点（活跃天数 / 连练 / 最高连击 / 最常玩 / 可复习项，全成长向、capped 4、从不空）；`parentPraise(idx)` 确定性挑一句家长寄语。`renderParentWeekly()`（`#module-parentreport`）：屏幕显示<b>四个大数字 tile</b> + 趋势 + 最常玩 chips + 亮点列表 + 寄语，并在 `<canvas>` 上画一张同款卡片（渐变背景 + 大数字 + 亮点 + 趋势 + 覆盖 + 寄语）可 ⬇ 保存 PNG / 📤 Web Share（不支持回退下载，复用演奏卡同款导出模式）。任意练习后经 `parentWeeklyOnUpdate`（接在 `recordPractice` 链，无新埋点）即时刷新。<b>服务家庭北极星「连接」</b>：给家长一扇了解孩子状态的窗（喂"家长认可"），而非 KPI 考核——少练的一周只读作"下周的新机会"。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 🌟 成长之星用 `microstars.js`（纯逻辑，12 单元测试）：灵感 Meludia——`MicroStars` 注入存储+时钟，`STAR_THRESHOLDS=[1,5,12,25,45,75,120,200]` 定义 8 颗星的「累计练对数」门槛，`starsForValue(v)` 纯函数算已点亮星数，`toNextStar(v)` 算距下一颗还差多少。`sync(id,value,label)` 用某技能最新累计练对数刷新星数<b>只升不降</b>（`max(prevStars,earned)`，值也取 max），返回 `{stars,prevStars,gained:[新点亮的星号],justMaxed}`。app 级单例 `microStars` + `syncMicroStars(moduleId,label)`：从 `practiceStats.moduleStats()` 取该模块累计 `correct` 喂入，点亮新星弹 `cheerToast`、集满 8 颗额外撒花；接在唯一漏斗 `recordPractice()` 内自动覆盖所有练习。`renderMicroStars()` 用 `HEATMAP_CATALOG` 同一份 14 项 level-1 技能目录（与热力图共用，保证<b>初级分类一致</b>），`all()` 排序为<b>星少的排前面</b>（最需鼓励的优先），每卡画金色实星★ + 灰空星☆ + "再练对 N 个点亮下一颗"，顶部「已点亮 X/Y 颗星 · N 个满星」金色进度条。设计要点=<b>胜利时刻 ×8</b>、第一颗星超易得，专治易放弃。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 🎸 乐队伴奏用 `backing-band.js`（纯逻辑，11 单元测试）+ 增强的 `midi-file.js`（捕获 program-change）：`midi-file.js` 在 `0xc0` program-change 事件处不再跳过，而是记录 `channelPrograms`（每通道首个 GM 音色号）与 `trackPrograms`（每音轨首个音色号）并随结果返回——这是<b>原解析器丢失的乐器信息</b>，乐队伴奏靠它给各声部分配音色。`backing-band.js`：`partKeyMode(parsed)` 判断用通道还是音轨当"声部键"（用到 ≥2 个通道就按通道，否则按音轨——format-0 单轨多通道文件靠这条正确拆分）；`splitParts(parsed)` 按声部键分组、通道 9 = GM 鼓、`gmFamily(program,isDrum)` 把 program 号映射成 10 个乐器家族（钢琴/贝斯/吉他/风琴/弦乐/铜管/管乐/合成/打击/其它，每家族带 `wave`波形+`gain`音量+`attack/release`包络），按平均音高降序返回；`suggestMyPart(parts)` 推荐音域最高的非鼓声部为"我来弹"（若最高声部音符过少则改取音符最多的旋律声部）；`buildSchedule(parsed,{myPart,rate,mutedParts})` 把音符分成 `mine`（我弹的，屏幕引导不发声）与 `backing`（乐队，电脑发声），时间按 `1/rate` 缩放（慢练），`mutedParts` 可再静音（如关鼓）。`renderBackingBand()`：<b>关键决策——乐队声部走独立 Web Audio 合成，绝不经 `playTone`/`emitPreviewMidi` 发往 CA99</b>（否则会抢占真琴，而真琴要留给 Lily 弹）。专用 `bandVoice(rec,whenSec)` 直接在 AudioContext 上建振荡器（按家族选波形/包络），鼓用噪声缓冲 + 带通滤波（低 midi=底鼓低频/高 midi=镲高频）；所有 backing 音用 `osc.start(startAt+ms/1000)` <b>精确排程</b>（采样级对齐，比 rAF+performance.now 更稳，Lily 跟得准），rAF 只驱动落音/键盘视觉。开头可选 4 拍 count-in（`clickAt` 方波点击声）。Lily 在真琴上弹时经新增全局钩子 `bandOnNote`/`bandOnNoteOff` 做<b>无压力命中反馈</b>（音高+时间窗匹配就飘 👍🎵⭐ 并计 hits，不扣分）。选曲弹窗复用 `.ps-modal` 样式 + `parseDirListing`/`buildUserCatalog`/`catalogFromManifest`：📚 内置（离线 `makeBandDemo()` 小星星合奏[旋律+贝斯+鼓]保证不联网也能玩 + CA99 catalog.json 1543 首）/ 🎵 我的 MIDI（扫 `data/`）/ 📤 上传。<b>坑点</b>：切换"我来弹"声部时早期误调 `loadParsed()` 会重跑 `suggestMyPart` 覆盖用户选择——已拆出 `applyPart()`（只重建计划+重排键盘、不重新推荐）专供切声部用，`loadParsed` 仅在载入新曲时调。情感价值=让易放弃的孩子<b>跟着整支乐队合奏</b>而非孤单重复。默认归为 🌱 初级（侧栏 `data-level="1"`）
- 节奏跟拍用 `rhythm-trainer.js`（纯逻辑，119 单元测试）：`beatsToOnsets` 把拍点位置按 BPM 换算成毫秒落点，`RhythmTrainer.tap(time)` 把每次敲击匹配到最近未命中落点并按时间误差 `rateError` 判完美(±55ms)/良好(±120ms)/多敲，`finish` 统计漏拍与平均误差；UI 有预备拍节拍器引导、播放头动画、落点高亮，note-on 或空格键触发敲击，完成后写入成就仪表盘
- 旋律听写用 `melody-dictation.js`（纯逻辑，486 单元测试）：`degreeToMidi` 把音阶级数（含跨八度/下行）映射成 MIDI，`MelodyDictation.next()` 在所选调音阶内生成首音为主音的随机短旋律，`play(note)` 逐音校验复奏（`samePitchClass` 支持忽略八度），整条全对才计分、有错完成不计分、可 `giveUp` 看答案；UI 用 Web Audio 三角波播放旋律、屏幕琴键、note-on 复奏，完成后写入成就仪表盘
- 和弦进行练习用 `chord-progression.js`（纯逻辑，120 单元测试）：`chordForDegree` 按调的音阶+各级品质把罗马数字级数（I/ii/.../vii°）展开成具体和弦，`expandProgression` 把整条进行换调展开，`ChordProgression.check(notes)` 复用 `chord-detect.js` 的 `detectChord` 比对根音+类型（忽略转位）判推进，整条走完计圈、可循环、`miss` 记错并清连击；UI 用 Web Audio 试听整条、罗马数字+和弦名 chip 高亮当前目标、"替我弹"演示推进，完成后写入成就仪表盘
- 节拍稳定度分析用 `beat-stability.js`（纯逻辑，58 单元测试）：`BeatStability.tap(t)` 采集击键时间戳，`iois` 算相邻间隔，`bpm` 取间隔中位数抗离群估速，`cv`（标准差/均值）量化波动，`stabilityScore` 把 CV 线性映射成 0–100 分，`trend` 用间隔的线性回归斜率判赶拍/拖拍/稳定，跟拍模式 `targetEval` 算对照目标 BPM 的平均绝对误差与准度；UI 实时条形图（按偏差染色）、节拍器试听、note-on 或空格键采集，完成后写入成就仪表盘
- 双手协调练习用 `hands-sync.js`（纯逻辑，58 单元测试）：`handOf(note, split)` 按音高分左右手，`clusterByTime` 把相邻击键聚成一拍，`evalBeat` 判断该拍是否双手到齐并算落键时间差 spread，`spreadScore` 把 spread 线性映射成协调度；`HandsSync.feed(note,t)` 累计当前拍、超过窗口自动结算上一拍，统计 `goodBeats/streak/best/avgScore/bothHandsRate`；UI 圆形仪表 + 逐拍色点，note-on 喂入或左右手按钮模拟，完成后写入成就仪表盘
- 琶音跑动测试用 `arpeggio-runs.js`（纯逻辑，90 单元测试）：`buildArpeggio(root,quality,octaves,direction)` 由根音+和弦性质（`CHORD_INTERVALS`）+八度+方向生成目标 MIDI 音序，`ArpeggioRuns.feed(note,t)` 按序校验（hit/miss/done），记录正确音之间的 IOI；`speedNps` 算每秒音数、`evennessScore` 把 IOI 变异系数线性映射成均匀度，综合分=均匀 60%+速度达标 40%−错误扣分；UI 目标音序高亮推进 + 进度条，note-on 喂入或点音序模拟，完成后写入成就仪表盘
- 连奏/断奏控制用 `articulation.js`（纯逻辑，60 单元测试）：`legatoRatio(duration,ioi)=按住时长÷到下一音起音间隔`，比值≈1 为连奏、<0.4 为断奏；`ArticulationTrainer.noteOn/noteOff(note,t)` 同时吃按键与松键事件，每个音在「时值已知 + 下一音已起」时结算，按目标 `legatoScore`/`staccatoScore` 线性打分；这是首个用到 note-off 时间的模块，UI 逐音色块分数墙 + 连奏/断奏模拟按钮，完成后写入成就仪表盘
- 踏板配合时机用 `pedal-timing.js`（纯逻辑，70 单元测试）：切分踏板法 = 弹新音→抬延音踏板→重新踩下接住新音；`PedalTiming.noteOn(note,t)` 记新音、`pedal(isDown,t)`/`feedCC(value,t)` 吃 CC64，检测"抬起后重新踩下"的瞬间与新音的间隔 gap=tRepress−tNote，`pedalScore`/`classifyPedal` 判干净（音后 40–220ms）/脏（踩太早）/干（踩太晚）；这是首个用到延音踏板 CC64 时机的模块，UI 踏板状态动画 + 净/脏/干色点 + 三档难度窗口，完成后写入成就仪表盘
- 颤音速度训练用 `trill.js`（纯逻辑，77 单元测试）：颤音 = 两个相邻目标音（lower/upper）之间快速来回交替；`TrillTrainer.feed(note,t)` 吃每次敲击，`trillHz(iois)`=1000/均值/2（每秒交替次数），`evennessScore` 用相邻击键间隔(IOI)的变异系数CV判均匀度；弹非目标音=wrongNote、连续同一目标音=repeat（没交替），综合分=均匀50%+速度达标50%−错音×6−没交替×4；UI 双键速度计可视化 + 下方音/音程/敲击数/目标速度选择 + 双键模拟，完成后写入成就仪表盘
- 装饰音训练用 `ornament.js`（纯逻辑，88 单元测试）：`buildOrnament({type,main,direction,interval})` 构造目标音序列——倚音 grace=[辅,主]、波音 mordent=[主,辅,主]、回音 turn=[上辅,主,下辅,主]；`OrnamentTrainer.feed(note,t)` 逐音匹配目标序列，弹错音=wrongNote（不推进序列），全部命中后用相邻音间隔(IOI)的均值算 `crispScore`（越快越干脆，≤crisp给100、≥3×crisp给0）+ `evennessScore` 算均匀度，综合分=干脆50%+均匀50%−错音×8；UI 目标序列逐音点亮 + 类型/主音/方向/音程/干脆度选择 + 一键模拟弹奏，完成后写入成就仪表盘
- 音程大跳准确度用 `leap.js`（纯逻辑，70 单元测试）：`buildLeaps({low,high,count,minLeap,rand})` 生成相邻间隔≥minLeap 的目标音序列（rand 可注入便于测试）；`LeapTrainer.feed(note,t)` 逐音匹配，弹对目标音前若没弹错=firstTry 一次弹准，弹错=miss（不推进、需继续找到目标），准确度 accuracy=一次弹准数÷总数（即综合分），同时记失误数 misses 与平均跳度 span；UI 目标序列点亮（绿）/抖动（红 miss）+ 音域/跳度/个数选择 + 一键模拟弹准，完成后写入成就仪表盘
- 旋律声部突出用 `voicing.js`（纯逻辑，63 单元测试）：`scoreVoicing(chord, {targetVoice,margin})` 评估一个和弦 `[{note,vel}]`——`targetIndex` 找目标声部（top=最高音/bottom=最低音），diff=目标力度−其余音最大力度，diff≥margin 给 100、diff≤0（旋律被埋没）给 0、线性；`VoicingTrainer.feed(note,vel,t)` 把窗口内的音聚成和弦、`flush()` 结算，跑满 rounds 个和弦后汇总平均分与达标数（≥80）；UI 和弦力度色块 + 每和弦评分点 + 旋律突出/埋没两种模拟，完成后写入成就仪表盘
- 力度渐变曲线用 `crescendo.js`（纯逻辑，70 单元测试）：`idealRamp(start,end,count)` 生成首尾间线性力度斜坡（画理想参考线）；`rampScore(velocities,{direction,minSpan,smoothTol})` 给一串力度打分——方向正确度 monotonic=相邻力度差符号与期望（渐强+1/渐弱−1）一致的比例（权重 0.5）、平滑度 smoothness=与首尾线性斜坡的平均绝对偏差映射（0.3）、跨度 spanScore=首尾力度差÷minSpan 封顶（0.2）；`CrescendoTrainer.feed(velocity)` 累积 count 个力度后自动 `_finish()` 结算并回调 onComplete，记 best/rounds，reset 清当前曲线保留统计；UI 用 SVG 实时画力度折线（绿/红点标每步方向对错、虚线为理想斜坡）+ 方向/音数/跨度选择 + 平滑/忽强忽弱两种模拟，完成后写入成就仪表盘
- 速度渐变用 `tempo-ramp.js`（纯逻辑，72 单元测试）：`toIois(times)` 把击键时间戳转相邻间隔、`ioiToBpm(ioi)=60000/ioi`；`tempoScore(times,{direction,minRatio,smoothTol})` 给一串时间戳打分——方向正确度 monotonic=相邻 IOI 差符号与期望（渐快 accel 间隔变小=−1 / 渐慢 rit 间隔变大=+1）一致的比例（权重 0.5）、平滑度 smoothness=IOI 与首尾线性斜坡的平均"相对"偏差映射（除以均值抗速度量级，0.3）、变速幅度 spanScore=首尾速度变化量÷minRatio 封顶（0.2）；`TempoRampTrainer.feed(time)` 累积 count 次击键后自动结算，记 best/rounds；UI 用 SVG 实时画 BPM 折线（绿/红点标每步方向对错、虚线为理想斜坡）+ 方向/敲击数/变速幅度选择 + 空格键敲击 + 平滑/忽快忽慢两种模拟，完成后写入成就仪表盘。与"节拍稳定度"（追求匀速）互补
- 复节奏用 `polyrhythm.js`（纯逻辑，66 单元测试）：`buildVoiceOnsets(taps,period,cycles,start)` 为一个声部生成理想落点时间戳、`combinedGrid(a,b,period)` 把 a:b 两声部在一周期内的落点合并成排序网格（教学/可视化用）；`PolyrhythmTrainer` 持两个 `VoiceState`（A/B），`tap(voice,time)` 只在该声部内把敲击匹配到最近未命中落点（声部隔离——左手敲击不消耗右手落点），按 `rateError` 判 完美(≤55ms)/良好(≤120ms)/多敲；`summary()` 综合分=命中率 70%+时间精度 30%（时间精度=1−平均误差÷good 窗口）；UI 双横向轨道按比例铺落点、播放头随预览+正式段推进、命中点实时高亮，连琴按中央 C 分左右手（note<60→A）、无琴用按钮或键盘 F/J，先听一遍预览再跟敲，模拟一遍写入成就仪表盘。与"节奏跟拍"（单声部）互补
- 颗粒性用 `evenness.js`（纯逻辑，57 单元测试）：核心是变异系数 `cv(arr)=stddev/mean`（越小越均匀）；`evennessScore(events,{velTol,ioiTol,velWeight})` 取一串音的力度数组与相邻间隔 IOI 数组各算 CV，再 `cvToScore(cv,tol)=clamp(1−cv/tol)` 线性映射成力度均匀分/时值均匀分（默认 tol=0.22 即 22% 离散为 0 分），综合分=力度分×velWeight+时值分×(1−velWeight)；同时给出每个音相对均值的"相对偏差"数组（velDev/ioiDev）供 UI 柱状着色（绿=齐、橙=偏）。`EvennessTrainer.feed(note,vel,time)` 累积 count 个音后自动结算，记 best/rounds；UI 双排柱（力度高度∝velocity、间隔高度∝IOI）实时生成、力度权重可调，连琴喂真实力度+时间、按钮/空格只喂固定力度（只练时值），模拟一遍写入成就仪表盘。与"力度控制"（打目标档）、"节拍稳定度"（跟匀速拍）互补——这里只看"一串音内部是否一致"，与绝对速度无关
- 手指独立性用 `finger-independence.js`（纯逻辑，44 单元测试）：`evaluateIndependence(events,{held,pattern})` 在 note-on/off 事件流上重放并维护"当前按下键集合"，每遇到一个移动音（非 held 的 on）就快照 `held.every(h=>down.has(h))` 记入 `heldDownAtTap`，独立保持率=全按住的敲击数÷移动敲击数；音型正确率=移动音逐位与 pattern（循环展开）相符的比例；综合分=独立保持×0.6+音型×0.4；另统计 `slips`（held 音在最后一个移动音之前被松开的次数，收尾正常松开不计——靠预扫描的 `lastMovingTime` 区分）。`FingerIndependenceTrainer.noteOn/noteOff(note,time)` 累积到 `targetTaps=pattern.length*reps` 个移动敲击后自动结算，记 best/rounds；UI 5 套预设（按 C+E 动 G 等）、按住键/移动键实时高亮，连琴检测按住与松开、模拟一遍演示评分（含偶发滑脱），成绩入仪表盘。与"双手协调""琶音跑动"互补——这里专练"按住的不动、该动的动"
- 音阶八度跨度用 `scale-span.js`（纯逻辑，62 单元测试，复用 evenness 的 `cv`/`toIois`、scale-trainer 的 `SCALE_TYPES`/`rootPitchClass`）：`buildSpan(root,type,octave,octaves,direction)` 生成跨 N 个八度的音阶 MIDI 序列（上行/上下行不重复顶点），`crossingIndices(seq,rootPc)` 找出每次跨入新八度根音的下标（穿指点）；`evaluateSpan(events,{expected,crossings})` 三段打分——音符正确率（逐位对，0.5）+速度均匀度（整串 IOI 的 `cv` 经 `1−cv/evenTol` 映射，0.3）+穿指衔接（穿指点进入音的 IOI 是否 > 中位 IOI×hitchRatio=卡顿，按未卡顿比例，0.2）。`ScaleSpanTrainer.feed(note,time)` 累积到 total 个音自动结算，记 best/rounds；UI 琴键条按八度铺开、穿指点橙边、跟弹实时高亮命中/错音，连琴依次弹、模拟一遍（含偶发穿指卡顿与错音）演示评分，成绩入仪表盘。与"音阶练习"（只判音准/进度）互补——这里多看"跨八度的连贯与均匀"
- 节奏听写用 `rhythm-dictation.js`（纯逻辑，60 单元测试）：`generatePattern(rng,level)` 按难度生成一串时值（以八分音符为 1 单位，3 档可用 0.5~4 单位），`iois` 取时值序列去掉末位即相邻间隔；`patternToOnsets(durations,bpm,start)` 供 UI 用 Web Audio 播放。核心 `evaluateDictation(targetIois,userIois,{tol})` <b>与速度无关</b>：先把用户总时长缩放对齐到目标总时长（`scale=sum(target)/sum(user)`）去掉速度因素，再逐间隔算相对误差 `relErr=|scaledUser−target|/target`、`1−relErr/tol` 映射成 0~1 分取均值得节奏准确度；个数匹配用 Dice 系数 `2k/(n+m)`（多敲/少敲都打折），综合分=节奏准确度×个数匹配度×100。`RhythmDictationTrainer.feed(note,time)` 只看时间不看音高，采满 `expectedTaps`（=音符个数）自动结算、也可少敲后手动 `finish()`，记 best/rounds；UI 听完点亮圆点、对比条画"目标 vs 你的"长短，连琴敲、模拟一遍演示评分，成绩入仪表盘。与"节奏跟拍"（看谱跟节拍器判绝对落点）互补——这里凭听、判相对长短
- 移调视奏用 `sight-transpose.js`（纯逻辑，55 单元测试）：旋律用相对主音的半音偏移表示（与调无关），`transpose(offsets,rootMidi)` 移到某主音，`intervals(seq)` 取相邻音程。`evaluateTranspose(played,expected,{octaveFlexible})` 把用户弹的音和"移到目标调的期望序列"逐音比——音级正确率（默认忽略八度，用 pitch class 比）×100 即综合分，多弹按 `min(0.3, extra×0.1)` 轻度扣分；另算<b>形状正确率</b>（相邻音程序列匹配比例）与<b>起音是否对</b>，当形状≥0.8 但音级<0.6 且起音错时置 `wrongKey` 提示"旋律对了但调没移对"。`SightTransposeTrainer.feed(note)` 采满 `total`（=音符数）自动结算、也可少弹手动 `finish()`，记 best/rounds；UI 展示原调音名片段+目标调、可🔊听原调（Web Audio 三角波音序）、👁看答案、弹奏实时填色（对绿错红）、模拟一遍演示评分，成绩入仪表盘。和"移调器"（整体升降键盘）、"视奏闪卡"（照谱原样弹）、"旋律听写"（凭听复奏原音高）都不同——这里练"看谱即时移调"
- 和弦转位听辨用 `chord-inversion.js`（纯逻辑，47 单元测试）：`QUALITIES`=大三[0,4,7]/小三[0,3,7]，`INVERSIONS`=原位/第一转位/第二转位。`buildInversion(rootMidi,intervals,inv)` 把最低 `inv` 个音各升一个八度再升序排列，得到三种排布；`stackIntervals(notes)` 取相邻半音间隔。判别"信号"：原位=两个三度叠（无纯四度）；第一转位=纯四度在<b>最上方</b>；第二转位=纯四度在<b>最下方</b>。`ChordInversionGame` 沿用耳训 API：`next()` 返回和弦音、`check(answerInv)` 判分、`score/streak/best/attempts/accuracy/reset()`、`onNew`/`onResult` 回调；根音范围钳制使最高音≤127。UI 仿耳训多选题——🔊播放和弦（可琶音/柱式，Web Audio 三角波）、三个转位按钮答题、答完揭晓音名（最低音高亮 `.inv-bass` 低音）、统计入仪表盘。和"和弦构建"（按名弹、忽略转位）、"和弦进行"、"和弦识别"都不同——这里专练转位的<b>听觉辨识</b>
- 调号识别用 `key-signature.js`（纯逻辑，402 单元测试）：基于五度圈，`SHARP_ORDER`=F C G D A E B、`FLAT_ORDER`=B E A D G C F；`accidentalList(count,type)` 按书写顺序生成升降记号、`keyForSignature(count,type,mode)` 反查调名（大调升号表 C G D A E B F# C#、降号表 C F Bb Eb Ab Db Gb Cb；小调为各自关系调）、`signatureFor(key,mode)` 正向查调号并与前者互为逆运算（测试做了全表 round-trip）。`hintFor()` 生成识别诀窍文字（升号调=最后升号上方半音、降号调=倒数第二个降号、1 降号固定 F）。`scaleMidi(key)` 用 `MAJOR_STEPS`/`MINOR_STEPS` 给出一个八度音阶供🔊播放。`KeySignatureGame` 沿用耳训/转位的 API：`next()` 随机出题（可配 `modes`/`maxAccidentals`/`includeNatural`/`choiceCount`，干扰项取五度圈相邻调）、`check(answerKey)` 判分、`score/streak/best/accuracy/reset()`。UI 仿多选题——显示调号 ♯/♭ 记号、四选一、答完高亮正确项+给诀窍提示+播放该调音阶，成绩入仪表盘。和"音阶练习""和弦识别"都不同——专练<b>看调号秒认调名</b>
- **88 键统一配色（dark theme，全练习共用，`app.css`）**：用 `:root` token 统一——`--kb-w-rest`/`--kb-b-rest`（静止白/黑键，白键为<b>柔和浅灰</b>非纯白，dark theme 不刺眼）、`--kb-press-glow`（按下信号色＝<b>霓虹青</b>，dark theme 下最醒目、对小孩最"酷炫"）、`--kb-hl`（答案高亮默认色，模块用 `--hl` 覆盖）。三种状态语义：<b>静止</b>（柔灰/深黑）→<b>高亮答案 `.kb-hl`</b>（"该弹这个键"：彩色描边光环＋轻染 token 色，<b>不再整键填白</b>）→<b>按下 `.kb-press`/`.kb-demo`</b>（霓虹青发光＋ `kbPop` 弹一下动效）。<b>坑点（与 scf-note n-left 同类的优先级 bug）</b>：`.kb-press` 与 `.kb-hl` 都设 `background`、同特异性时<b>源码靠后者赢</b>——曾因 `.kb-hl` 写在 `.kb-press` 之后，导致<b>按下被高亮答案的白底盖掉</b>（真实弹键看不清）。修复＝把 `.kb-press` 块放到 `.kb-hl` <b>之后</b>且 `z-index` 更高（6 > 5），按下永远盖过高亮
- **真实 MIDI 输入接线（所有练习通用）**：全局 `onMidiIn` 把真琴 note-on 分发给各模块的<b>识别钩子</b>（`sightOnNote`/`scaleOnNote`/… 多数练习已覆盖，物理弹奏即判分）。两处通用机制让"屏幕 88 键"在<b>每个</b>练习都跟随真琴：(1) `piano-keyboard.js` 维护静态实例表 `PianoKeyboard.instances`，`echoOn/echoOff` 把真琴按下/松开<b>点亮所有"当前可见"键盘</b>（`isVisible()` 用 `offsetParent` 判可见）——之前只有曲谱跟弹有 `scfKbEcho`，现已通用化；(2) 少数"点击琴键即作答"且<b>没有全局钩子</b>的练习（音名键名 `ni-kb`、五线谱→键 `sr-kb`、MIDI 播放器练习 `mpl-kb`）在键盘 opts 加 `recognizeExternal:true`，`onMidiIn` 把真琴音<b>路由到该键盘的 `onNoteOn`</b>（等价于点击），用全局 `suppressTone` 标记抑制合成音（真琴已发声，避免重音）。<b>坑点</b>：① 识别钩子与 `recognizeExternal` 路由<b>二选一</b>，不能同时给同一键盘，否则一次弹奏判分两次；② 改 ESM 模块后浏览器会因 HTTP 启发式缓存复用<b>旧的嵌套 import 模块单例</b>（`node --check` 用 CommonJS 也测不出 ESM 报错），冒烟测试要换端口=新 origin 强制全量重载；③ <b>跨模块 element id 撞车（真琴输入"按了没反应"的隐蔽根因）</b>：早期多个模块用了相同的 2 字母 id 前缀，导致 5 处 `id` 全局重复——🎸乐队伴奏 vs 🐉Boss战(`bb-kb`)、颤音 vs 渐速(`tr-*`)、音阶跨度 vs 视奏乐句(`sp-*`)、和弦进行 vs 五度圈拼图(`cp-*`)、和弦转位内部冗余复制(`inv-kb`×2)。因为 `$('#id')`/`querySelector` 只返回 DOM 中<b>第一个</b>匹配，Boss战的 `new PianoKeyboard($('#bb-kb'))` 实际绑到了乐队伴奏那块<b>隐藏的窄键盘</b>上 → 高亮/`echoOn` 点亮的是看不见的键，用户看到的 88 键"永不亮、弹了不前进"，必须点屏幕才行；而 🚀极速挑战用唯一前缀 `sr-` 故正常——这正是"为何 speedrun 好、boss 坏"。<b>排查法</b>：浏览器里跑 <code>document.querySelectorAll('[id]')</code> 统计重复 id（应为空）。<b>修复</b>：把冲突一方整体改唯一前缀（boss→`bsb-`、渐速→`trr-`、视奏乐句→`sph-`、五度圈拼图→`cfp-`，按 token 边界精确替换避免误伤 `bb-kbcap`/`cresp-score` 等子串），并删掉和弦转位的重复键盘块。<b>新增模块务必用全局唯一的 id 前缀</b>
- **调试钩子**：无真机时控制台调 `window.__feedMidi(note, velocity)` 模拟弹奏、`window.__feedNoteOff(note)` 模拟松键、`window.__feedCC(controller, value)` 模拟踏板/控制器，测试依赖 MIDI 输入的模块
- **预听/示范为何默认在 CA99 真琴发声（🎬 演奏台）**：绝大多数模块预听只调 `playTone()`（纯浏览器 Web Audio 振荡器，<b>从不</b>发 MIDI out），所以即便钢琴连着也只有电脑出声。要在 CA99 上响必须发 note-on/off 到<b>输出端口</b>：`kbMidiOn(midi,ch,vel)`/`kbMidiOff` → `midi.send(bytes)`。输出端口在连接时由 `midi-core.js` 的 `autoSelect` 自动选中（webmidi 看 `midi.output`、桥接看 `midi._client && midi._connected`）。原「曲谱跟弹」把它做成默认<b>关</b>的「🎹 真琴发声」开关，且只有它接了线。<b>演奏台默认开</b>：用 `voice(midi,durMs,vel)` 统一发声——真琴可用就只走真琴（更真实音色、避免与浏览器重音），未连接才回退 `playTone`。跟弹模式下物理按键本身已在真琴响，只对<b>屏幕点击</b>补发声。<b>坑点</b>：判断输出是否就绪不能只看 `realPiano` 标记，要 `midiOutReady()` 真查端口，否则 `realNoteOn` 静默失败、浏览器也不响 = 完全没声。
- **全局「预听走真琴」默认开（所有模块通用）**：上面只覆盖了演奏台/曲谱跟弹，但用户希望<b>每个</b>练习的预听/示范都默认在 CA99 上响。做法是在<b>全局 `playTone(freq,startOffset,dur,gainPeak)` 里集中下沉</b>——全 app 66 处预听都经它发声，且全部传 `midiToFreq(n)`（无裸频率 UI 提示音），故可反推 MIDI 音高：`midiNote = round(69 + 12*log2(freq/440))`。新增 `emitPreviewMidi()`：`previewOnPiano` 开关 + `midiOutReady()` 端口就绪时，反推音高（限 21–108）、由 `gainPeak` 估力度（`vel=clamp(40+gainPeak*240,20,120)`）、`kbMidiOn` 发声、按 `dur` 调度 `kbMidiOff`（`_previewTimers` Map 处理重叠同音），<b>返回 true 则 `playTone` 跳过电脑振荡器</b>（真琴当音源，避免电脑三角波盖过真琴）。未连接时 `emitPreviewMidi` 立即返回 false → 照常用振荡器 → <b>没接琴时零行为变化</b>。顶栏加 `#preview-piano-toggle`「🎹 预听走真琴」复选框（默认开、记忆到 localStorage、关时清掉挂起的 note-off）。<b>防重音坑点</b>：自己已管真琴发声的模块（曲谱跟弹的示范/提示）会同时调 `playTone` 和 `realNoteOn`，故用 `suppressMidiEcho` 标记包住其 `playTone`，并把它们改成「`realPiano && midiOutReady()` → 仅 `realNoteOn`（真琴）；否则 → `suppressMidiEcho` 包住的 `playTone`（仅电脑）」的<b>互斥二选一</b>，杜绝同一音电脑+真琴双响；曲谱跟弹的「🎹 真琴发声」默认也随之改为<b>开</b>，与其他模块一致。`suppressTone`（物理键路由抑制合成音）为真时 `emitPreviewMidi` 也返回 false。
- **初级模块体验增强（鼓励反馈 + 配色可视度）**：逐项 review 19 个初级练习后做了一批<b>适度增强</b>。① 新增<b>全局可复用庆祝助手</b> `cheerBurst(host,n)`（向任意元素撒一阵彩屑，复用 `.scf-confetti` keyframes）+ `cheerToast(text,host)`（鼓励飘字，`.cheer-toast`/`cheerPop` 动画，host 为 static 时自动设 `position:relative`），接进原本只有文字反馈、缺庆祝动效的练习：音阶练习完成（零失误撒 90 片+「完美！」）、键盘音名认知<b>连击每 5 次</b>弹「连击 N！🔥」、音阶指法提示完成（满分撒花）。② <b>配色可视度统一</b>：音阶高亮键由偏暗的 `#5b8cff` 提亮到 `#7ea8ff`（dark theme 下更清楚）、卷帘/五线谱播放器左手色由 `#c084fc` 提亮到 `#d59bff`、节奏跟拍播放头由 `#667eea` 细线改 `#22d3ee` 青色加粗加辉光。③ 音色浏览器加 <b>🎲 随机</b>按钮（随机选一个音色并试听，`.ghost-btn` 样式）+ 选中卡片 `soundActivePulse` 呼吸光。<b>坑点</b>：庆祝助手是 ESM 模块作用域函数，<b>不挂在 `window`</b> 上（`page eval` 取到 `undefined` 属正常），冒烟测试验证视觉效果要么注入 DOM 节点核对 CSS 动画名，要么走真实模块交互触发。
- **连胜宽恕 + 欢迎回来（#4，practice-stats 引擎扩展）**：连胜不再「漏一天就清零」。① `dayStreak(opts)` 新增可选 `{forgive}` 宽恕额度——允许跨过 N 个「漏练单日」而不中断（连胜冻结/freeze）；`forgive=0` 与原严格语义完全一致（<b>成就判定仍用严格值</b>，不放水），UI 显示传 `forgive:1`，漏 1 天连胜照常累计，并在数字后加 🛡️ 标记「冻结生效」。② 新增 `lastActiveBeforeToday()` / `comebackGap()`：当今天练过、但最近一次练习在 2 天前（昨天没练），横幅改显示 `👋 欢迎回来！新的连胜从今天开始 🌟`（绿色），用温柔问候替代冷冰冰的「连练 1 天」，对久别归来零惩罚。新增 12 条单元测试（practice-stats 51→63）。
- **先听后弹（#5，铃木教学法门槛，🎬 演奏台）**：跟弹一首<b>还没听过</b>的曲子时，先自动放一遍<b>完整示范</b>（先在脑中建立声音印象再动手），听完<b>自动接入跟弹</b>。实现：`🎧 先听后弹` 开关（默认开）+ `previewed` Set 记已听曲目（key=`song.id||song.title`）+ `autoFollowAfter` 标志；点「🎯 跟弹」时若该曲未听过则改 `start('demo')`，`stop(finished)` 里示范<b>完整放完</b>才 `previewed.add` 并在 700ms 后自动 `start('follow')`，中途停止则取消自动跟弹；已听过的曲子再点跟弹直接开始。
- **三色反馈：弹错音 vs 没跟上（#7，score-follow 引擎扩展）**：原来弹错键（音高错）被<b>静默忽略</b>，孩子不知道「我到底哪里错了」。现在 `judge(midi,t)` 在没命中任何目标时，再判断<b>此刻是否确有一个该弹的音</b>：若有 → 返回 `{wrong:true, due}`（音高错，`wrong` 计数 +1、断连击、但<b>不消耗</b>该目标音符，可重弹）；若此刻无音该弹 → `{wrong:false}`（纯多弹，不惩罚）。UI 据此分三色：✅ 弹对 = 绿/青（PERFECT/GOOD）、❌ 弹错键 = <b>红色「弹错音了」</b>并把<b>正确的键</b>闪绿提示、⏱️ 过窗没跟上 = <b>粉色「节奏没跟上」</b>（落音块 MISS 也从红改粉），让孩子一眼分清「弹错」与「慢了」。演奏台与曲谱跟弹同步生效；新增 3 条单元测试（score-follow 33→36）。
- **彩色音符脚手架，练熟后自动淡出（#8，`note-color.js`）**：启蒙识谱模块（👀 视奏闪卡 + 🎼 五线谱识谱卡）给五线谱上的音符头<b>按音名上色</b>——七个白键走彩虹（C 红 → D 橙 → E 黄 → F 绿 → G 蓝 → A 靛 → B 紫，Boomwhackers 教学法），帮零基础孩子把「五线谱位置 → 音名 → 键位」对应起来。<b>关键：脚手架会撤掉</b>——`scaffoldStrength({correct,attempts})` 随正确率给出 1→0 的不透明度：样本不足（<8 次）或正确率没过 85% = 满色；越练越准 → 颜色越淡；接近全对 → 几乎隐去（回到普通音符，逼孩子真正学会读谱、不依赖颜色）。`🌈 彩色音符` 开关（默认开、localStorage 记忆、两模块共享 `ca99-color-scaffold` 键）；音符头通过内联 `fill`/`opacity` 上色（淡尽时回退 CSS 默认色）。纯逻辑 `note-color.js`（`PC_COLORS[12]`/`noteColor`/`scaffoldStrength`/`isWeaned`）+ 7 条单元测试。
- **🕵️ 猜歌视奏游戏壳（`guess-song.js`）**：把视奏变成"破案"——`GuessSong` 三阶段状态机 `idle→play→guess→done`：`next()` 随机选一首<b>公有领域童谣</b>（`SONGS` 8 首，开头乐句）+ `quizOptions(correctId, 4, rng)` 生成 4 个打乱选项；`play` 阶段 `press(midi)` 逐音校验（`octaveAgnostic` 默认开、弹错 `idx=0` 从头来不惩罚），走完整段 → `phase='guess'`；`guess(id)` 判断曲名，对 → 计分（首次答对 +10、`_guessWrong` 过则 streak 清零、不得分），错 → 留在 `guess` 可重猜。`rng` 可注入保证测试确定性。UI（`renderGuessSong`）：藏名 `🕵️ ？？？` 条 + <b>多音符五线谱</b>（自绘 SVG 治理高音谱号 + 加线，当前音黄/已弹绿）+ 88 键<b>等待模式高亮该弹的键</b>；弹完<b>自动回放旋律</b>（`setTimeout` 链 + `playTone`→真琴优先）+ 浮现 4 选项 chip；答对揭晓曲名（金色 `revealed`）+ `cheerToast`+`cheerBurst`+`recordPractice`，答错 chip 变灰红、温和提示、可🔊再听。CA99 物理输入经新增 `guessOnNote`/`guessOnNoteOff` 钩子（与屏幕点击同一条 `handlePress`）。12 条单元测试，成绩入仪表盘。默认归为 🌱 初级（侧栏 `data-level="1"`）。
- **🎯 练习宾果游戏壳（`bingo-card.js`）**：把枯燥的"练习清单"做成宾果——`BingoCard` 用 `shuffle(TASK_POOL, rng)`（Fisher–Yates）从 23 个多样小任务池抽 24 个填 5×5 网格、中心为 ⭐ 免费格（构造时即 `marked`）。`toggle(i)`/`mark(i)` 切换/点亮一格后 `_evaluate()` 用 `winLines(size)`（5 行+5 列+2 斜线=12 条）检测新完成的线，只对<b>首次</b>完成的线报 `newLines`（取消点亮会撤销该线）。`isFull()`/`doneCount()`/`lineCount()` 供统计。`rng` 可注入保证测试确定性。UI（`renderBingoCard`）：5×5 网格（emoji+任务文字、免费格金色），点亮 → 弹跳动画 + ✓ + 绿色；连线 → `cheerToast`+`cheerBurst`+该线金色高亮；填满 → 大庆祝 + 全卡高亮；当天进度按 `ca99-bingo-YYYY-MM-DD` 存 localStorage（隔天自动换新卡、当天刷新还原已点亮格 + 已连线高亮），可手动「🔄 换新卡」。纯逻辑、无需键盘/MIDI 钩子（自报告式打卡），成绩入仪表盘。11 条单元测试。默认归为 🌱 初级（侧栏 `data-level="1"`）。
- **🎲 骰子热身游戏壳（`dice-warmup.js`）**：把"今天练什么"的<b>选择压力</b>（决策疲劳对易放弃孩子尤其劝退）交给骰子——`rollMission(rng)` 从 `KEYS`(6 调) × `PATTERNS`(音阶↑/↓/↑↓/琶音) × `HANDS`(左/右/双) × `FLAVORS`(连奏/断奏/弱→强/慢慢来/数拍子) 各随机一面，可弹音型 `pattern.gen(rootMidi(key.pc))` 生成音符序列。`DiceWarmup` 状态机：`roll()` 掷新卡、`press(midi)` 逐音校验（`octaveAgnostic` 默认开），弹对走完整段 → `done++`、本张无错则 `streak++`（弹过错则清零），弹错 `idx=0` 从头来（不惩罚）。注入式 `rng` 保证测试确定性。UI（`renderDiceWarmup`）：掷骰动画 + 四色任务卡（调/音型/手/风味分面）+ ✅完成数 / 🔥连续无错 + 乐句进度 chips（当前音黄色高亮）+ 88 键<b>等待模式高亮该弹的键</b>；命中闪绿、错音闪红 + 温和提示、完成撒花 + `cheerToast` + `recordPractice`。CA99 物理输入经新增 `diceOnNote`/`diceOnNoteOff` 钩子（与屏幕点击同一条 `handlePress`）。12 条单元测试，成绩入仪表盘。默认归为 🌱 初级（侧栏 `data-level="1"`）。


## 连接方式（默认双支持）

`midi-core.js` 用 `navigator.requestMIDIAccess({sysex:true})` 枚举所有端口——**USB 和蓝牙 MIDI 都在列表里**，运行时在顶栏下拉选择，或自动选含 "CA99"/"Kawai" 的端口。无需改代码切换连接方式。

### 蓝牙 MIDI 桥（Windows）

Windows 上 Chrome/Edge 的 Web MIDI 走 WinMM，**看不到蓝牙 (BLE-MIDI)**。`bridge/` 下提供一个 Python-`winrt` 桥（走 WinRT，能看到蓝牙且自动重组分片 SysEx）。前端**零改动**复用：从局域网 IP 打开页面时 `midi-core.js` 自动用 `ws://<本页host>:8765` 连桥——**远程设备只需打开 `http://<机器IP>:8099/`**，不必手填 `?bridge=`；也可显式 `?bridge=1` / `?bridge=ws://host:port` 或 `localStorage 'ca99.bridgeUrl'`；本机 localhost 默认走 Web MIDI（`?bridge=0` 可强制 Web MIDI）。USB-B 连接最简单、延迟最低、**不需要桥**。详见 `../bridge/README.md`。

## 前置依赖

| 用途 | 需要 | 说明 |
|------|------|------|
| 运行 app | **Chrome 或 Edge** | Web MIDI 仅 Chromium 系支持；Firefox/Safari 不行 |
| 起本地服务器 | **Python 3** 或 Node | 任选其一起静态服务器（Web MIDI 需 http/https，不能 file://） |
| 跑单元测试 | **Node ≥ 16** | 测试是原生 ESM（`.mjs`），无需安装任何 npm 依赖 |
| 真机 USB 验证脚本 | **Python 3 + mido + python-rtmidi** | 仅 `scripts/validate_ca99_usb.py` 需要，见下方 |

> 本项目零构建、零 npm 依赖：HTML/CSS/JS 直接跑，测试用 Node 内置能力。

## 安装与运行

```bash
# 0. 克隆仓库
git clone https://github.com/lg-o1/ca99-control.git
cd ca99-control/app

# 1. 本地起静态服务器（二选一；Web MIDI 需要 http/https，不能 file://）
python -m http.server 8099        # 用 Python
#   或
npx http-server -p 8099           # 用 Node

# 2. 用 Chrome 或 Edge 打开（Firefox/Safari 不支持 Web MIDI）
#    http://localhost:8099/index.html

# 3. 点"连接" → 授权 MIDI（含 SysEx）→ 选 CA99 端口 → 玩
```

**不连钢琴也能用**：所有训练模块（视奏/听辨/力度/音阶/节奏/和弦）都能在没有真机时玩——
听辨/听写用 Web Audio 发声，节奏跟拍可用空格键敲击，其余可在浏览器控制台用调试钩子
`window.__feedMidi(note, velocity)` / `window.__feedNoteOff(note)` / `window.__feedCC(cc, val)` 模拟弹奏。

手机：Android Chrome 同样可用（USB-OTG 或蓝牙 MIDI）。

## 测试

**一键跑全部 58 套测试**：

```bash
cd app
# Bash / Linux / macOS
for f in js/*.test.mjs; do node "$f"; done

# 或 PowerShell（Windows）
Get-ChildItem js\*.test.mjs | ForEach-Object { node $_.FullName }
```

全绿即每行输出 `xxx: N passed, 0 failed`。单独跑某一套：

> ⚠️ **检查 `app.js` 语法务必用模块模式**：`Get-Content js/app.js -Raw | node --input-type=module --check`。
> 直接 `node --check js/app.js` 按 CommonJS 解析，会漏掉某些只在 ES module（严格模式）下才报的错（例如误删 `function xxx() {` 导致的括号失衡），结果是浏览器加载时整段 `main()` 静默失败、所有模块空白。

```bash
# 协议库单元测试（20 用例）
node js/ca99.test.mjs
# 自动换音色引擎单元测试（21 用例）
node js/auto-rotate.test.mjs
# VT 渐变引擎单元测试（32 用例）
node js/vt-morph.test.mjs
# 力度换音色路由单元测试（33 用例）
node js/velocity-switch.test.mjs
# 力度→VT 联动单元测试（22 用例）
node js/vel-vt-link.test.mjs
# 踏板控制单元测试（27 用例）
node js/pedal-control.test.mjs
# 演出预设存储单元测试（40 用例）
node js/preset-store.test.mjs
# 和弦识别单元测试（46 用例）
node js/chord-detect.test.mjs
# 和弦练习挑战单元测试（32 用例）
node js/chord-trainer.test.mjs
# 节拍器 + 速度检测单元测试（33 用例）
node js/metronome.test.mjs
# 录制 + SMF 编码单元测试（38 用例）
node js/recorder.test.mjs
# 音阶练习单元测试（41 用例）
node js/scale-trainer.test.mjs
# 视奏闪卡单元测试（62 用例）
node js/sight-reading.test.mjs
# 音程听辨单元测试（49 用例）
node js/ear-training.test.mjs
# 力度练习单元测试（52 用例）
node js/dynamics-trainer.test.mjs
# 移调器单元测试（74 用例）
node js/transposer.test.mjs
# 练习成就仪表盘单元测试（51 用例）
node js/practice-stats.test.mjs
# 节奏跟拍训练单元测试（119 用例）
node js/rhythm-trainer.test.mjs
# 旋律听写单元测试（486 断言）
node js/melody-dictation.test.mjs
# 和弦进行练习单元测试（120 用例）
node js/chord-progression.test.mjs
# 节拍稳定度分析单元测试（58 用例）
node js/beat-stability.test.mjs

# 双手协调练习单元测试（58 用例）
node js/hands-sync.test.mjs

# 琶音跑动测试单元测试（90 用例）
node js/arpeggio-runs.test.mjs

# 连奏/断奏控制单元测试（60 用例）
node js/articulation.test.mjs

# 踏板配合时机单元测试（70 用例）
node js/pedal-timing.test.mjs
node js/trill.test.mjs
node js/ornament.test.mjs
node js/leap.test.mjs
node js/voicing.test.mjs
node js/crescendo.test.mjs
node js/tempo-ramp.test.mjs
node js/polyrhythm.test.mjs
node js/evenness.test.mjs
node js/finger-independence.test.mjs
node js/scale-span.test.mjs
node js/rhythm-dictation.test.mjs
node js/sight-transpose.test.mjs
node js/chord-inversion.test.mjs
node js/key-signature.test.mjs
node js/scale-fingering.test.mjs
node js/interval-build.test.mjs
node js/mode-id.test.mjs
node js/solfege.test.mjs
node js/chord-quality.test.mjs
node js/progression-ear.test.mjs
node js/cadence.test.mjs
node js/note-id.test.mjs
node js/staff-read.test.mjs
node js/sight-phrase.test.mjs
node js/chord-sight.test.mjs
node js/rhythm-sight.test.mjs
node js/accompaniment.test.mjs
node js/chord-color.test.mjs
node js/piano-keyboard.test.mjs
node js/score-follow.test.mjs
node js/midi-file.test.mjs
node js/userlib.test.mjs
node js/circle-of-fifths.test.mjs
node js/bridge-protocol.test.mjs
```

## 数据生成（如需重建）

```bash
python ../scripts/build_ca99_data.py   # 从 reference/ 提取 CA99 专属数据到 app/data/
```

## ⚠️ 待真机验证

协议库已通过单元测试（组帧字节正确），但**逆向的 SysEx 字节尚未在真 CA99 上验证**。

### 快速验证：Python USB 脚本

`scripts/validate_ca99_usb.py` 基于 `app/data/sounds.json` + `sysex.json`，向真机发"切到
Concert Grand"的标准 Bank Select + Program Change（可选再发一条 CA99 SysEx 开启
Rendering），用来确认逆向数据在真机上有效：

```bash
pip install mido python-rtmidi          # 安装 MIDI 后端
python scripts/validate_ca99_usb.py --list          # 1) 看有哪些 MIDI 输出口
python scripts/validate_ca99_usb.py                 # 2) 自动找 CA99 口，切到 Concert Grand
python scripts/validate_ca99_usb.py --sysex         # 3) 切音色 + 发 SysEx 开启 Rendering
python scripts/validate_ca99_usb.py --dry-run --sysex   # 只打印字节不发送（无需硬件/依赖）
```

USB 线把 CA99 的 "USB to Host" 接电脑、开机后运行。若钢琴面板音色变成 Concert Grand、
声音对，即说明逆向的 `msb/lsb/pc` 与 SysEx 帧格式在真机上验证通过。

### 其他验证途径
1. USB 连真 CA99 → 打开 app → 音色浏览器点一个音色 → 看琴是否真换音色
2. 若不换，对照 `reference/appui-full/lib/kawaipianojs/kawaipiano.js` 的 getMidi() 核对组帧
3. 或用 MIDI 监视器：在钢琴面板手动改设置，看回传的 SysEx，反推正确字节

